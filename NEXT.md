# Очередь задач для следующих сессий

Файл существует ради экономии токенов: вместо того чтобы заново разбираться
в проекте, следующая сессия берёт отсюда готовый промпт и сразу работает.

**Правило.** Заканчивая сессию — обнови этот файл: вычеркни сделанное
(удали блок целиком), допиши то, что всплыло и осталось несделанным.
Промпт пишется так, чтобы его можно было скопировать в чистую сессию
и не объяснять контекст: путь, файлы, что проверить, чем закончить.
Если задача большая — делегируй её агенту, а не тяни в основную сессию.

Актуально на 12.09.2026.

---

## 3. МЕЖДУ СТРОК молчит: база цитат израсходована

**Почему важно:** в `data/subtext.json` три записи, и все три помечены `used`.
Рубрика в очередь больше не попадает вовсе. Пополняется она только руками
и намеренно: легальных источников для текстов песен нет, а выдуманная строчка
убивает доверие быстрее всего (см. CLAUDE.md).

```
Проект «ПЛЁНКА»: папка ПЛЕНКА в домашней директории. Общайся по-русски.

Рубрика МЕЖДУ СТРОК (prompts/rubrics/subtext.md) стоит: в data/subtext.json
все записи израсходованы. Задача — пополнить базу.

Правила, которые нельзя нарушать:
- В поле lines кладутся ТОЛЬКО реально существующие строки, которые ты можешь
  подтвердить источником. Не помнишь дословно — не пиши; тогда пусть lines
  будет пустым, а разбор пойдёт по notes (так рубрика тоже работает).
- В notes — проверяемые факты: что за трек, когда вышел, что вокруг него
  происходило. Никаких догадок о намерениях артиста.
- artist пишется ровно так, как имя стоит в data/artists.json.

Разберись с форматом по существующим записям, предложи владельцу 5-7 кандидатов
(артист, трек, на чём строится разбор) и спроси, какие брать, — вкус тут
решает он. После подтверждения добавь записи и проверь генерацию:
python -m src.compose --dry-run (модель не вызывается).
```

---

## 4. Русской сцены нет в RSS — нужен другой источник

**Почему важно:** проверено ~90 адресов. The Flow ленту не отдаёт нигде,
Rap.ru переехал на Tilda, у SRSLY ленты нет, Hip-Hop.ru и Звуки отвечают
403, остальные — JS-сайты с пустым RSS. Из живого добавлены `rap-music.ru`
и `eatmusic.ru`, но первый пишет раз в месяц. Канал теперь пишет про русский
рэп, а инфоповодов по нему взять неоткуда: в срочные новости лезет эстрада.

```
Проект «ПЛЁНКА»: папка ПЛЕНКА в домашней директории. Общайся по-русски.

RSS по русской рэп-сцене вымер (подробности — в data/feeds.json, у мёртвых
лент проставлена причина). Нужен другой механизм добычи инфоповодов.

Продумай и предложи владельцу варианты, с оценкой сложности и рисков:
- публичные Telegram-каналы сцены через веб-превью t.me/s/<канал> (без API,
  обычный HTTP + разбор HTML) — насколько это стабильно и законно;
- ВКонтакте: wall.get по сообществам сцены (токен у проекта уже есть,
  см. src/vk.py);
- новинки релизов как инфоповод (iTunes/Deezer уже собираются) — то есть
  вместо новостей о сцене писать о том, что вышло;
- SerpAPI (ключ есть в .env, кодом пока не используется).

Не бросайся кодить: сначала короткое сравнение и вопрос владельцу, каким
путём идти. Помни ограничения проекта: пять зависимостей в requirements.txt,
всё крутится на GitHub Actions, состояние — JSON в репозитории.
```

---

## 5. Непарный тег от модели убивает пост молча

**Почему важно:** проверено вживую на Bot API 10.09.2026. Ошибку разбора
вызывает не амперсанд (`Simon & Garfunkel`, `R&B;`, «хвост &» API принимает
как есть), а `<`: голый знак «меньше» уже экранируется в `sanitize`, но
незакрытый или перекрёстный тег — нет. `<b>жир` без закрывающего отвечает
`can't parse entities: Can't find end tag`, и пост не уходит вообще,
без записи в архив. В 78 постах архива и очереди такого пока нет, но
`sanitize` — защита именно от непредсказуемого вывода модели.

```
Проект «ПЛЁНКА»: папка ПЛЕНКА в домашней директории. Общайся по-русски.

В src/telegram.py функция sanitize() чистит разметку перед отправкой.
Голый «<» она экранирует (_BARE_LT), самопроверка — python -m src.telegram.
Незакрытый или перекрёстный тег она пропускает, а Telegram на нём отвечает
«can't parse entities» и не публикует пост вовсе — молча, без архива.
Воспроизводится на «незакрытый <b>жир» и «<i>курсив</b>».

Сравни два пути и выбери короткий:
1. Досчитывать пары в sanitize (стек открытых тегов, лишние — вырезать).
2. Запасной путь в telegram._call: если API ответил «can't parse entities»,
   повторить один раз без parse_mode и с вырезанными тегами. Дороже одним
   запросом, зато закрывает и те поломки, которых мы не придумали.

Проверь живьём на TELEGRAM_ADMIN_ID (сообщения за собой удаляй через
deleteMessage) и допиши случаи в _selftest внизу src/telegram.py.
```

---

## 6. Мемы: вид готов, смешно пока через раз

**Почему важно:** мем — главный источник роста, его пересылают чужим. Вид
владелец утвердил 11.09.2026, а юмор нет: первые пачки вышли тухлыми
(итоги года, «слушателей за месяц: 41», цифры вместо шутки). После того как
правила мема переписали вокруг его рефа, из шести мемов понравились три.
Владелец пришлёт ещё рефы — с ними и доводить.

```
Проект «ПЛЁНКА»: папка ПЛЕНКА в домашней директории. Общайся по-русски.

Сначала проверь, влита ли в origin/main ветка post-form (рубрика МЕМ с картинкой:
data/memes.json, assets/meme/templates/, card.render_meme). Если нет — работай
в её рабочей копии .claude/worktrees/post-form, в устаревшее дерево ~/ПЛЕНКА
не пиши: с 12.09.2026 оно выровнено по origin/main, правки там потеряются.

Что уже есть. Мем выходит картинкой: шаблон из каталога data/memes.json
(47 штук, key/name/when/source), надпись на самой картинке сверху и снизу
белым гротеском с тонкой обводкой, водяной знак канала в углу, текст поста —
подписью под фото. Модель отвечает полями top, bottom, text, picture
(src/llm.py, схема мема). Правила — prompts/rubrics/meme.md. Вид утверждён,
его не трогай.

Задача — чтобы мемы были смешными. Материал:
1. Реф владельца: ~/Downloads/photo_2026-09-11 13.19.13.jpeg — 3D-яблоко
   с руками, сверху «Иногда я говорю «хз»», снизу «Но я знаю». Его слова:
   «почти не имеет смысла, но смешнее». Это направление, а не шаблон для всех.
2. Новые рефы — спроси у владельца, куда он их положил. В репозиторий их
   не коммить: это чужие мемы, они нужны только для разбора.
3. Вердикт владельца на пачку по новым правилам. Понравились:
   - енот застрял | Слушаю альбом с середины / Начало я не заслужил |
     подпись: Первый трек включу, когда стану лучше
   - курица из Minecraft | Знаю все слова этой песни / Ни одного правильно |
     подпись: Пятнадцать лет пою свою версию. Оригинал уже звучит как кавер
   - рыба смотрит | Ставлю трек на повтор / Чтобы он ко мне привык |
     подпись: Сегодня он поздоровался первым
   Не то совсем:
   - курица | Слушаю грустное на кухне / Мне норм | Сосед стучит в стену. Тоже, наверное, слушает.
   - енот | Слушаю один трек четвёртый месяц / Остальные берегу | Плейлист на 600 песен. Открываю ради одной
   - рыба | Слушаю один трек сорок минут / Он длится две | На сорок первой минуте начинаю понимать слова
   Гипотеза для проверки: в удачных поворот одушевляет или выворачивает логику
   (начало надо заслужить, трек должен привыкнуть), в неудачных — буквальная
   констатация. Ещё три видимых дефекта: четыре надписи из шести начинаются
   со «Слушаю», картинки в пачке повторяются, в подпись пролезают цифры.

Порядок: разбери рефы и вердикт → сформулируй механику → перепиши meme.md
(удачные мемы — в образцы, неудачные — в антиобразцы) → добавь в каталог
картинки под рефы (только реальные мем-картинки, не генерация нейросетью,
без встроенного текста, без политики и смерти) → добей разнообразие в пачке
(не повторять картинку и первое слово надписи в одном прогоне compose.plan).

Проверка: 6–8 мемов через compose.generate_checked("meme", payload) без записи
в очередь, отправь их владельцу в личку (publish.send на TELEGRAM_ADMIN_ID)
все подряд, без отбора, и спроси вердикт. Повторяй, пока он не скажет «да».
В канал ничего не публикуй, content/queue не трогай, без его согласия не пушь.
```

---

## 7. Релизы: дубли в рассылке, потерянные совместные, утверждение, очередь запусков

**Почему важно:** решения владельца от 11.09.2026 сделаны. От выхода релиза до поста
не больше суток. Пост пишет каждый сбор, по одному на релиз: РЕЛИЗ или ВЕРДИКТ,
жребием по весам (compose --fresh в collect.yml и urgent.yml). Выходит он своим
выходом раз в час (publish --releases): через 2 часа на полный трек или сразу
с треком. С 4-го за московские сутки пост идёт без звука, и обычные посты в такие
сутки уступают (publish.due). С 23:00 до 9:00 по Москве любой пост молчит
(config.QUIET_FROM_HOUR). Остались хвосты.

```
Проект «ПЛЁНКА»: папка ПЛЕНКА в домашней директории. Общайся по-русски.
Работай в свежем клоне origin/main, не в ~/ПЛЕНКА.

Контекст: src/collect.py (collect_releases, own_release), src/compose.py
(fresh_releases, release_key, release_jobs, do_fresh), src/publish.py (next_post,
due, releases_today), src/service.py (notify_releases), data/inbox.jsonl
(merge=union — старые строки не переписывать, фильтровать при чтении).
Проверка вживую:
  python -m src.compose --fresh --dry-run
  python -m src.publish --releases --dry-run

1. Дубли в рассылке. Посты сверяют «Slipknot — Arsenal» и «Arsenal - Single»
   по compose.release_key, а collect кладёт в inbox оба, и service.notify_releases
   шлёт подписчику весть по каждому отпечатку — дважды. Сверять тем же ключом
   в collect_releases или в самой рассылке.
2. Режим admin. При PUBLISH_TARGET=admin ежечасный выход шлёт владельцу на
   утверждение один и тот же пост о релизе каждый час, пока тот не нажмёт кнопку.
   Нужна отметка в посте, что он уже ушёл на утверждение.
3. Потерянные совместные. Соавтора iTunes по id не отдаёт, его ищем по имени,
   и при другом написании релиз отсеивается: «Ramil' & Basta — Сияй» не прошёл,
   потому что в data/artists.json стоит «Баста». Варианты: поле aliases
   в artists.json (дёшево, ручное) или сверка по contributors Deezer, у которого
   id есть у всех основных артистов. Гостей (role Featured) по-прежнему не брать.
   «Ramil' & Basta — Сияй» до сих пор отсеивается и в compose --fresh --dry-run.
   Той же природы: collect.own_release отсеивает совместные, где наш артист второй
   и записан иначе, — «Долги» Смоки Мо в магазине «Vito & Smoky Mo»; такого релиза
   нет ни в свежих постах, ни в previous_releases.
4. Очередь запусков. У группы state-write GitHub держит только один ожидающий
   запуск, а новый вытесняет его. Кроны опаздывают на часы: 11.09 сбор с расписанием
   00:17 шёл в 04:40, срочные на 17:20 — в 19:47. Поэтому ежечасный publish --releases
   (:50) может отменить ожидающий сбор или срочные, и находка опоздает на 6 часов
   (запас до суток около 7). Вариант: выпускать релизы из дежурства (moderate.serve,
   раз в 10 минут рядом с push_state) — оно и так публикует по кнопке и пушит состояние.

Каждый пункт — assert в compose._selftest или publish._selftest на синтетических
записях, без сети. Проверки: python -m src.compose --selftest,
python -m src.publish --selftest. В канал ничего не публикуй, compose без --dry-run
не запускай.
```

---

## 12. Всё актуальное — с минимальной задержкой

**Почему важно:** решение владельца от 11.09.2026: новости, инфоповоды и всё, что
привязано ко дню, должно выходить с минимальной задержкой. Релизы уже укладываются
в сутки (задача 7), а остальные пути никто не мерил.

```
Проект «ПЛЁНКА»: папка ПЛЕНКА в домашней директории. Общайся по-русски.
Работай в свежем клоне origin/main, не в ~/ПЛЕНКА.
Кода не пиши, пока владелец не ответит.

Для каждой рубрики и каждого пути посчитай задержку от события до поста
по расписанию, обычную и худшую. Время в .github/workflows указано по UTC,
аудитория живёт по Москве. GitHub опаздывает с кронами на часы: у сбора,
стоящего на 00:17, в inbox бывает collected_at 04:41.

Пути:
1. ИНФОПОВОД — src/urgent.py в urgent.yml (06:20, 11:20, 17:20). Окно
   URGENT_MAX_AGE_HOURS = 36, между вечерним и утренним заходом 13 часов.
   config.URGENT_PER_RUN = 1, а urgent.yml по расписанию передаёт COUNT = 3
   (inputs.count || '3'). Какой лимит задуман?
2. news в очереди: вес 0 в config.RUBRICS, plan их не пишет. Проверь, что старых
   news в content/queue не осталось: 10.09 вышла новость от 25.08.
3. ЛЕГЕНДА и годовщины: calendar.yml в 05:20. calendar_check кладёт пост в очередь
   с префиксом 0000-, и он выходит ближайшим обычным слотом. Но publish.due
   считает все публикации: выход релиза или срочная новость за 3 часа до слота
   отодвигают годовщину. Правило «сутки отданы релизам» её не держит, интервал держит.
4. Срочная новость в 17:20 пишется в data/posted.json и отнимает у обычного поста
   слот 18:05 (PUBLISH_INTERVAL_HOURS = 3).
5. Истории: stories.yml в 07:30, stories.pick_post берёт пост из очереди.
6. clips --news: расписание в clips.yml выключено. Как свеж inbox к ручному запуску?
7. Викторина: quiz.yml в 12:00. Подтверди, что ко дню она не привязана.

Итог — таблица «путь → обычная и худшая задержка → что её задаёт → как сократить
и чего это стоит» и один вопрос владельцу, что менять. Ничего не публикуй,
compose и urgent запускай только с --dry-run, getUpdates не вызывай.
```

---

## 8. Хвосты поста о релизе: длинный текст, ВКонтакте, рамка

**Почему важно:** с 12.09.2026 пост о релизе — одно сообщение (publish.send): обложка
в рамке канала (card.cover), текст и площадки строкой в нём же. Кнопок нет, плеера рядом
нет, полный трек уходит первым комментарием (comments.seed). Осталось несколько мест,
где музыка или рамка молча не доезжают.

```
Проект «ПЛЁНКА»: папка ПЛЕНКА в домашней директории. Общайся по-русски.
Работай в свежем клоне origin/main.

1. ~~Подпись к медиа в Telegram — не больше 1024 знаков.~~ Закрыто 12.09.2026:
   лимит считается по видимому тексту, а не по разметке (telegram.visible_len),
   и строка площадок в него почти не входит. Ужимать промпты не понадобилось.
2. Отрывок магазина в ленту больше не идёт: `compose.fresh_preview`, поле `preview`
   в постах и `--backfill-music` остались от прежнего вида. Проверь, нужен ли отрывок
   кому-то ещё (СЛЕПАЯ ПРОСЛУШКА берёт свои), и что не нужно — убери.
3. ~~Во ВКонтакте музыка не уходит~~ — и не сможет: wall.post отказывает
   и треку, и плейлисту (проверено 12.09.2026). Ссылка на подборку сообщества
   стоит в тексте записи, обложка уходит вложением.
4. Рамку обложки проверили только на обложке iTunes: CDN Deezer из домашней сети
   отвечает 403, и дома card.cover возвращает None — уходит голая обложка по ссылке.
   Посмотри в канале первый релиз с обложкой Deezer и первую срочную новость
   с портретом (urgent.with_portrait): в рамке ли они.
Проверки: python -m src.publish --dry-run, python -m src.vk --check. В канал не публикуй.
```

---

## 9. Полный трек от владельца: хвосты

**Почему важно:** 11.09.2026 (коммит 5dd6033) бот научился принимать трек любым
файлом — mp3, m4a, mp4-видео, wav, flac, как audio или document. Файл он перезаливает
плеером с названием и артистом из поста и обложкой: магазин → вшитая → iTunes.
В тот же день выяснилось, что до владельца это не доходило: смена жила со старым
кодом (снимок на момент постановки в очередь) и не могла ни pull, ни push из-за
конфликта offset, а присланные треки молча пропали. Теперь смена стартует
с `git pull`, пуш кода в src/ ставит свежую смену, и идущая уступает ей место
(code_changed); файл не ответом на запрос тоже получает ответ. Осталось:

```
Проект «ПЛЁНКА»: папка ПЛЕНКА в домашней директории. Общайся по-русски.
Работай в свежем клоне origin/main. Приём трека — src/moderate.py
(track_file, normalize_track, attach_track), самопроверка: python -m src.moderate --selftest.

1. В _push_repo (src/moderate.py) конфликт при `git pull --rebase` молча оставляет
   ребейз висеть до конца смены. Нужен `git rebase --abort` и запись в лог.
2. На текстовый ответ вместо файла отвечать владельцу «жду аудиофайл».
3. Обложка из iTunes сверяется только по артисту — может встать обложка
   другого релиза того же артиста. Сверять ещё и название трека.
4. Лимит getFile в 20 МБ теперь касается и mp3: трек в 320 kbps длиннее ~8,5 минуты
   не примется. Честно предупреждать об этом в самом запросе трека.
Проверка — синтетическими апдейтами, getUpdates не вызывать. В канал не публиковать.
```

---

## 13. СЛЕПАЯ ПРОСЛУШКА: первый живой прогон титулов

**Почему важно:** 11.09.2026 (коммиты fee9112, 9d28eb3) викторина переехала
в комментарии: отрывок уходит в канал, неанонимный опрос под пересылкой вешает
дежурство, угадавшие назавтра получают метку «знаток: <артист>» (setChatMemberTag,
Bot API 9.5). Проверено только на поддельном Telegram. Первая загадка по новой
схеме — 12.09 в 15:00 МСК, первые метки — 13.09. Сломается молча: запасной путь
отдаст викторину в канал, и титулов просто не будет.

```
Проект «ПЛЁНКА»: папка ПЛЕНКА в домашней директории. Общайся по-русски.
Работай в свежем клоне origin/main. Прослушка — src/quiz.py (to_channel, attach,
take_answer, award), голоса ловит src/moderate.py. Загадка, опрос и угадавшие —
в приватном хранилище (config.PRIVATE: quiz.json и папка quiz/).

После 12.09 15:00 МСК:
1. Лог запуска «Слепая прослушка» (gh run list --workflow quiz.yml): викторина
   «в комментариях» или ушла в канал запасным путём. Если запасным — найти почему
   в логе дежурства (moderate.yml) около 15:00: пересылка, attach, _sync.
2. Под постом в чате «ПЛЕНКА 4AT» висит неанонимный опрос; в открытом
   data/quiz.json нет ни вариантов, ни имени артиста.
После 13.09 15:00 МСК:
3. В логе прослушки строка «Титул «знаток: …»: N из M угадавших». M=0 при живых
   голосах — не доходят poll_answer (allowed_updates, дежурство). «Метка не встала» —
   смотреть текст ошибки Telegram (длина, символы в сокращённом имени).
4. Спросить владельца, видна ли метка у угадавших.
Правки проверять python -m src.quiz --selftest. Меток реальным людям руками не ставить.
```

---

## 14. Claude Opus 5 вместо GigaChat — отложено владельцем

**Почему важно:** GigaChat выдумывает прошлое артистов: 11.09.2026 у Смоки Мо
«серия больших альбомов» (за лето вышло 4 сингла), у kizaru «первый трек начинается
с фразы, которой закончился прошлый альбом». Claude Opus 5 на тех же данных писал
по треклисту. В коде Claude уже подключён (src/providers/claude.py, claude-opus-5,
кэш голоса, пакет для обычной очереди), в воркфлоу ключ прокинут — не хватает
секрета и переменной. Владелец 11.09.2026 решил пока остаться на GigaChat.

```
Проект «ПЛЁНКА»: папка ПЛЕНКА в домашней директории. Общайся по-русски.
Делай только после «да» владельца на вопрос о цене.

1. Значение ANTHROPIC_API_KEY из ~/ПЛЕНКА/.env → gh secret set ANTHROPIC_API_KEY
   (в вывод не печатать). В collect, urgent, calendar, clips, compose и moderate
   ключ уже прокинут в env.
2. gh variable set LLM_PROVIDER --body anthropic. Запасной остаётся gigachat
   (vars.LLM_FALLBACK || 'gigachat') и поднимается только на технических отказах.
3. Докстринг src/providers/claude.py («нужен, если захочется сравнить качество»)
   поправить: теперь это основной генератор.
4. Проверка: после ближайшего сбора — лог compose --fresh
   (gh run list --workflow collect.yml): пишет Claude. Через сутки —
   расход в console.anthropic.com против оценки, доложить владельцу.

Оценка 11.09.2026: около $0,7 в день, $20 в месяц. Посты о релизах и срочные новости
пишутся сразу, по полной цене; пакетом, вдвое дешевле, идёт только обычная очередь.
В августе ушли на GigaChat, потому что «Claude недоступен в России» (коммит a533a04);
11.09 ключ из .env отвечал.
```

---

## 15. Первый пост о релизе по-новому: проверить в канале

**Почему важно:** 12.09.2026 пост о релизе стал одной плиткой: обложка в рамке, текст,
площадки строкой, ни кнопок, ни плеера. Полный трек уходит первым комментарием.
Проверено в личке и на поддельных апдейтах, в канале — ещё нет.

```
Проект «ПЛЁНКА»: папка ПЛЕНКА в домашней директории. Общайся по-русски.
Работай в свежей копии origin/main. Отправка — src/publish.py (send), первый
комментарий — src/comments.py (seed), зовёт его дежурство src/moderate.py (process).

После первого поста о релизе в канале (gh run list --workflow publish.yml, выход --releases):
1. Страница t.me/s/plenka_fm: пост одной плиткой, в конце текста строка «▸ Слушать — …»,
   под постом видна кнопка «Комментарии».
2. Журнал дежурства (moderate.yml) около времени поста: пришла одна пересылка, под ней
   первым комментарием встал полный трек (или вопрос, если трека к посту не было).
3. Рубрика и трек берутся из data/posted.json и архива, а дежурство подтягивает их
   git-ом прямо перед комментарием (push_state в moderate.process). Проверь по журналу,
   что подтягивание успевает: рубрика вопроса своя, трек тот самый.
getUpdates не вызывать, в канал ничего не публиковать. Правки проверять
python -m src.moderate --selftest.
```

---

## 16. Автопилот точности: первые запуски и хвосты ловли

**Почему важно:** владелец 11.09.2026 требует полный автопилот — посты сам не проверяет.
GigaChat выдумывает, поэтому выдумки ловятся в два слоя: грубо при генерации
(quality.problems, коммит d1d722c) и умно после — облачный Claude по подписке каждые
2 часа (рутина «ПЛЁНКА — автопилот точности», бриф prompts/review.md, правки применяет
review.yml). 11.09 в 21:50 МСК запущен разовый полный проход по очереди.

```
Проект «ПЛЁНКА»: папка ПЛЕНКА в домашней директории. Общайся по-русски.
Работай в свежей копии origin/main.

1. Первые запуски. Рутины — /schedule list, прогоны — RemoteTrigger list_runs по
   trig_01TiGEVWjZT1kPSUCgfYqMCW (каждые 2 часа) и trig_019DvLdRP3EKKubUrzp3UonY
   (разовый проход): быстрый выход срабатывает, правки доходят до ветки claude/review-*,
   review.yml (gh run list --workflow review.yml) применяет их и удаляет ветку.
   Ветка claude/review-* старше часа — признак вытесненного запуска в группе state-write.
2. Дыры грубой ловли (живой прогон GigaChat 11.09): «третья работа за лето», «ещё один
   короткий сингл», «закрепил тенденцию» проходят; «все треки укладываются в 3 минуты»
   и «меньше трёх минут каждый» с треклистом не сверяются (у kizaru три трека за 3:00).
   Добавлять только с новым замером потерь на тех же 4 заданиях — compose.generate_checked
   на отпечатках inbox 695a898a, 8f033904, e1f700d9 (РЕЛИЗ) и 8f033904 (ВЕРДИКТ).
3. Прощённый брак описи выходит с описью прозой («16 треков», «ЗА 38 МИНУТ» в заголовке):
   вырезается только строка <code>. Правит ли это автопилот точности — посмотреть.
4. previous_releases сверяет каждую старую строку inbox с магазином — запрос на строку
   при каждом посте. Начнёт тормозить сбор — кешировать.
5. После ближайшей публикации в канал: в новом посте content/archive есть поле message;
   после первого разбора, затронувшего архив, правка дошла до канала (t.me/s/plenka_fm).
6. Задание retry в review.yml перезапускает ветки через gh workflow run — ему нужно право
   actions: write, иначе упадёт и напишет владельцу. Задание notify проверить ручным
   запуском на ветке с заведомо битым файлом правок.
7. Сторож src/health.py красных запусков остальных воркфлоу не видит — сигналит только разбор.
8. Первый проход 11.09 (коммит 3e8725d) превратил вердикт nkeeei & Yanix в пустышку
   с цитатой «Дослушал до конца. Второй раз не стал.» — это реплика из образца формы
   в voice.md, а модель не слушала. Бриф поправлен (своего не дописывать, пустое снимать),
   пост снят. Реплики «от слушателя» в voice.md («неделю не снимаю с повтора») голос любит,
   а точность — нет: спросить владельца одним вопросом, оставлять ли их.
Проверки: python -m src.quality --selftest, python -m src.compose --selftest,
python -m src.review --selftest. В канал не публиковать, getUpdates не вызывать.
```

---

## 17. Разборы уходят в канал голым текстом — нужен кадр

**Почему важно:** референс канала (CODEINE CRAZY, 62.5K, владелец прислал 12.09.2026)
держится на том, что **под каждым постом есть картинка или видео** — пост без медиа
в ленте проматывают. У ПЛЁНКИ картинка есть только там, где пришла обложка релиза
(`post["cover"]`, см. `publish.send`): РЕЛИЗ и ВЕРДИКТ. ОТКУДА НОГИ и МЕЖДУ СТРОК —
главные рубрики канала по весу — уходят текстом.

```
Проект «ПЛЁНКА»: папка ПЛЕНКА в домашней директории. Общайся по-русски.
Работай в чистой копии от origin/main, оттуда коммить и пушь.

Задача: дать картинку разборам (ОТКУДА НОГИ, МЕЖДУ СТРОК), которые сейчас уходят
в канал голым текстом — в ленте их проматывают.

Что уже есть и переиспользуется, а не пишется заново:
- src/footage.py и src/clips.py уже ищут фотографию артиста по имени из data/artists.json;
- src/card.py умеет кадр в камкордерной обработке (card.cover — рамка для обложки);
- src/publish.py:send ставит фото, только когда у поста есть post["cover"].

Проверить: python -m src.publish --dry-run покажет, у каких постов очереди картинки нет;
закончить тем, что у разбора в очереди появляется кадр и он уходит подписью к фото
(caption ≤ 1024 знаков — длинные разборы придётся резать или слать текстом, как сейчас).
Правило канала: лица в кадре не генерируем, вещь важнее человека (см. CLAUDE.md).
```

---

---

## 18. Разовая дозагрузка треклистов к старым находкам

**Почему важно:** в `data/inbox.jsonl` 104 релиза, из них 30 без треклиста
и 6 с треклистом без отрывков. Посты по ним выходят беднее: писать не о чем,
кроме подписи и даты. Механизм перенесён 12.09.2026, но ни разу не запускался.

```
Проект «ПЛЁНКА»: папка ПЛЕНКА в домашней директории. Общайся по-русски.
Работай в свежем клоне origin/main.

Запусти python -m src.collect --backfill-tracks и закоммить data/inbox.jsonl.
Осторожно: файл переписывается целиком, а в .gitattributes у *.jsonl стоит
merge=union — параллельный автоматический сбор в это время задвоит строки.
Поэтому: сначала git pull, потом запуск, потом сразу коммит и push, и глянь,
не идёт ли в это время collect.yml (gh run list --workflow=collect.yml).
Проверь по паре дозагруженных релизов, что треки и отрывки на месте:
python -m src.compose --fresh --dry-run.
```

---

## 19. Автопилот точности не успевает: посты выходят с выдумками раньше правок

**Почему важно:** 12.09.2026 в канал ушли два поста с выдумками — своё
прослушивание («я поставил фоном, а на 3-м круге…») и мотивы артистов
(«одному нужна была песня с названием покруче»). Ловушки на оба класса
в `src/quality.py` добавлены в тот же день, но это лечит будущие посты.
Вопрос в другом: облачный автопилот (`src/review.py`, раз в 2 часа) их тоже
не поймал — ни до публикации, ни после.

```
Проект «ПЛЁНКА»: папка ПЛЕНКА в домашней директории. Общайся по-русски.

Разберись, почему автопилот точности (src/review.py, .github/workflows/review.yml,
бриф prompts/review.md) не поймал выдумки в постах 20260911-081829-1573-verdict
и 20260911-163734-3647-release: они лежали в очереди с 11.09 и вышли 12.09.
Посмотри запуски review.yml за эти сутки (gh run list --workflow=review.yml),
ветки claude/review-* и что в них было написано.

Ответь на два вопроса: успевает ли он по времени (пост из очереди может выйти
раньше, чем рутина до него дойдёт) и видит ли он такие выдумки вообще —
в брифе про намерения артистов и собственное прослушивание ничего нет.
Почини то, что окажется сломанным, бриф правь текстом, а не кодом.
```

## 20. Ролики с голосом владельца: первый прогон и хвосты

**Почему важно:** конвейер слит 11.09.2026 (src/reels.py, воркфлоу «Ролики»,
бриф prompts/reels.md). 12.09 руками отправлен первый сценарий
(`20260912-albums`, Кизару и Сода Лав): проверка, рассылка шести фраз владельцу
и удаление ветки прошли. Голосовые, сборка и пакет живьём ещё не проверялись.
По расписанию сценарии пишет облачный Claude — routine «ПЛЁНКА — сценарий
ролика» на claude.ai/code/routines, модель Opus, пн/ср/пт 9:00 МСК,
первый запуск 14.09.

```
Проект «ПЛЁНКА»: папка ПЛЕНКА в домашней директории. Общайся по-русски.

Ролики с живым голосом владельца. Облачный Claude по пн/ср/пт пишет сценарий
по prompts/reels.md и пушит в ветку claude/reels-*; воркфлоу «Ролики»
(.github/workflows/reels.yml) рассылает владельцу фразы по одной; владелец
отвечает голосовым на каждую; бот (src/moderate.py → src/reels.py) по полному
комплекту запускает сборку; в личку приходит пакет — ролик, превью, название,
описание, теги, время. Заливает владелец сам.

1. Хвост первого прогона (сценарий 20260912-albums): дошли ли голосовые
   до reels.accept, собрался ли ролик, пришёл ли пакет. Смотреть
   gh run list --workflow reels.yml и папку reels/<id>/ в приватном хранилище.
2. Подложки ждут в запросе berg-creator/plenka-state#1 (12 треков Pixabay
   в audio/). Не слит — ролики собираются без музыки. По audio/SOURCES.md
   треки не прослушаны, в dark-04 возможна голосовая бирка продюсера.
3. Повтор тем: автор сценариев не видит прошлых — они в приватном хранилище,
   ветки удаляются. Вести открытый список «id — тема — дата» (например,
   воркфлоу «Ролики» дописывает строку при рассылке) и велеть сверяться с ним
   в брифе.
4. После первого настоящего ролика послушать голос: к живому голосу
   применяется та же «телефонная» обработка VOICE_GRADE, что к синтезу
   (src/clips.py). Глухо — отключить для роликов.
5. Мем кладётся в верхнюю часть кадра, внизу пусто — поставить по центру
   или крупнее (src/reels.py).
6. «Выложить сегодня до 21:00» пишется буквально, даже если сборка ушла
   на следующий день — считать от времени сборки.
7. Кадр face берёт только имена из data/artists.json — это сужает темы.
   Решить, дописывать ли артистов в базу по ходу: сборка идёт из main.
8. Остатки синтеза голоса в .github/workflows/clips.yml и .env.example
   (FISH_AUDIO_KEY, kokoro, «голос Крис») — убрать.
9. На сервере висит ветка sync-clean-2026-09-11: пять коммитов про посты
   (трек во ВК, рамка обложки, ПРОЯВКА, --backfill-tracks, генераторы обложек),
   намеренно не слитых 11.09. Сверить с тем, что уже сделано в main, нужное —
   отдельным запросом, ветку удалить.

Проверки без трат: python -m src.reels --selftest, --check на сценарии,
сборка --preview с дублями из macOS `say -v Milena`. getUpdates и
moderate --serve локально не запускать — у бота один offset.
```

---

## Закрытые вопросы — не переоткрывать

- **Бот в личке остаётся с разбором строк из песен (/tekst), лимит — один разбор
  в сутки.** 12.09.2026 владелец выбрал этот вариант из трёх: потерянная ветка
  `sync-clean-2026-09-11` (коммит a85ce31) ставила главным слежение за релизами,
  убирала разбор текстов и поднимала лимит до трёх. Ветку в этой части не переносим,
  она больше ни для чего не нужна.

- **Музыку во ВКонтакте не приложить.** 12.09.2026 проверено на отложенной записи:
  `wall.post` от ключа сообщества отказывает и треку, и плейлисту, с фото и без —
  «Required a combination of photo attachments for audio attachment». Подборка
  сообщества уходит ссылкой в тексте, вложением идёт только обложка. Второй ключ
  от личного аккаунта заводить не будем — он живёт недолго и его надо продлевать.

- **Кнопок под постом нет вовсе, площадки — ссылками в тексте.** У поста канала
  с инлайн-кнопками Telegram не показывает комментарии — это ожидаемое поведение
  (bugs.telegram.org/c/41803). Панель из восьми кнопок к тому же читалась в ленте
  раньше самого поста. Владелец 12.09.2026: пожертвовать кнопками, но сделать пост
  красивее; страницу-прокладку выбора площадок (song.link) отвергли — в ней нет
  ни VK Музыки, ни Звука, и она уводит из Telegram.
- **Плеера рядом с постом нет, полный трек — первым комментарием.** Вид ленты не должен
  зависеть от того, прислали трек к релизу или нет (владелец 12.09.2026: «плеер ни к селу
  ни к городу»). Отрывок магазина в ленту не идёт совсем.
- **Описи в РЕЛИЗЕ и ВЕРДИКТЕ нет — ни строкой `<code>`, ни предложением.** Владелец
  сам вырезал её из поста Ghost Mountain 11.09.2026, а 12.09.2026 забраковал и её
  развёрнутый вид («самый долгий трек 3:07, самый короткий — 1:38»): цифры без вывода
  растягивают пост. Пересказ описи
  quality.problems сверяет с данными релиза; упрямый пост выходит без описи,
  а не пропадает.
- **Посты о свежих релизах и срочные новости пакетом не пишем.** Пакет вдвое дешевле,
  но ответ приходит через минуты, а бывает и часы. Владелец 11.09.2026: срочное
  выходит вовремя, а Claude без пакета (~$0,7 в день) пока дорого — остаёмся на GigaChat.
- **Дешёвые модели Claude ради экономии не берём.** 11.09.2026 на 4 постах (3 РЕЛИЗА
  и ВЕРДИКТ): Opus 5 — 7 запросов за $0,30, писал по данным; Sonnet 5 — 8 запросов
  за $0,18, выдумал Смоки Мо «Ю.Г. в 90-х»; Haiku 4.5 — 10 запросов за $0,055,
  потерял 3 поста из 4. Модель дешевле — посты хуже.
  Открытый репозиторий удешевляет только минуты Actions, токены — нет.
- **Опрос прослушки живёт в комментариях, а титул знатока выдаётся назавтра.**
  В канале голоса анонимны — бот не узнает, кто угадал. Метка, поставленная
  сразу, спалила бы ответ всем, кто ещё листает комментарии. Решено с владельцем
  11.09.2026, подробности — в докстринге src/quiz.py.
- **Полный трек бот сам не ищет — запрос уходит владельцу сразу.** Стриминги
  отдают 30 секунд, остальное — перезаливка без прав. Полный трек присылает
  владелец (задача 9).
  Легальные бесплатные источники проверены 11.09.2026 на 13 реальных постах
  и 10 артистах: 0 попаданий.
  - Bandcamp отдаёт бесплатно только релизы 3–9-летней давности. Тёзки опасны:
    sematary.bandcamp.com — группа из Новой Зеландии.
  - На SoundCloud у официальных треков скачивание выключено, скачать можно лишь
    перезаливы. Ключ API требует подписки Artist Pro.
  - Наших артистов нет в Jamendo и FMA, у FMA API мёртв.
  - Audius проверен 10.09.2026: там перезаливы («ghostemane-mercury_(Devmp3indir.Net)»
    от аккаунта с нулём подписчиков), не оригиналы.
  Бесплатное скачивание к тому же не даёт права выкладывать трек в канал.
  Владелец закрыл тему 11.09.2026. Ссылки на файлообменники и конвертеры бот
  тоже не подбирает — под запросом только магазин и поиск YouTube, SoundCloud, Bandcamp.
- **Логотипы стримингов на кнопках под постом в канале не ставятся.** Иконку
  на кнопке (icon_custom_emoji_id, Bot API 9.4) Telegram показывает в каналах
  только ботам с купленным на Fragment именем; Premium владельца открывает её лишь
  в личке и группах. Проверено 11.09.2026 по документации. Поэтому кнопки подписаны
  словами, по три в ряд: при четырёх подписи обрезались у всех, кроме VK.
- **song.link (Odesli) для выбора стриминга не годится.** 11.09.2026: публичный API
  отвечает 401 PUBLIC_API_ACCESS_DEPRECATED, а на странице выбора нет ни Spotify,
  ни Яндекса, ни VK — даже у ASTROWORLD. Кнопки собирает publish.listen: у магазина,
  откуда пришла находка, точная ссылка на релиз, у остальных — поиск.
- **RSS по русской рэп-сцене вымер.** 10.09.2026 проверено около 90 адресов, причины
  стоят у отключённых лент в data/feeds.json. Нужен другой механизм (задача 4).
- **Автозаливки роликов нет, голос в них живой.** 11.09.2026: ролики, залитые через
  YouTube Data API из непроверенного проекта Google, запираются приватными до аудита;
  Zapier — лишний посредник без отложки; браузерная автоматизация YouTube Studio
  нарушает условия площадки. Клипы ВКонтакте через API не заливаются — только
  истории, владелец проверял. Клон и синтез голоса отвергнуты: шутку держит
  интонация. Владелец заливает сам, бот присылает пакет (задача 20).
