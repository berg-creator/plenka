# Очередь задач для следующих сессий

Файл существует ради экономии токенов: вместо того чтобы заново разбираться
в проекте, следующая сессия берёт отсюда готовый промпт и сразу работает.

**Правило.** Задача выполнена — её блок удаляется сразу, тем же коммитом,
что и сама правка, а не «в конце сессии». Всплывшее и несделанное дописывается.
Номера не пересчитываются: на них ссылаются старые промпты.
Файл опустел — сказать об этом владельцу прямо, одной строкой: задачи кончились,
скажи, чем заняться дальше.
Промпт пишется так, чтобы его можно было скопировать в чистую сессию
и не объяснять контекст: путь, файлы, что проверить, чем закончить.
Если задача большая — делегируй её агенту, а не тяни в основную сессию.

Актуально на 18.09.2026.

## С чего начать новую сессию — эстафета

Эстафета — ради экономии токенов: одна сессия берёт одну задачу, читает только
её раздел и в конце отдаёт тот же промпт для следующей. Владелец копирует блок
ниже в чистую сессию, как есть, раз за разом. Порядок меняет основная сессия,
когда появляются новые задачи.

```
Проект «ПЛЁНКА», ~/ПЛЕНКА, по-русски. Эстафета задач. Экономь токены:
CLAUDE.md уже в контексте — не перечитывай; NEXT.md и GROWTH.md целиком
не читай: grep -n '^## ' NEXT.md и только раздел своей задачи; отчёты
docs/research/ — только если задача велит. Агентов и воркфлоу не заводи,
если раздел задачи прямо не просит. Проверка — сухими прогонами и --selftest.
Работай в чистой копии от origin/main (git worktree add … origin/main).

Порядок: 50а → 50б → 50г → 50д → 48 → 44 → 45 → 15 → 21
→ 24 → 18 → 16 → 17 → 8 → 6 → 42 → 50в → 50е → 39.
Ждут не времени, а владельца — не брать: 13, 14, 27, 31, 32, 40, 47, 50ж.

1. Возьми первую задачу из порядка, чей раздел ещё есть в NEXT.md, не помечен
   «Остановлено» и чьё условие «Делать, когда…» выполнено. Выполни её промпт.
2. Упёрлась в решение владельца или во время — строка в её раздел
   «Остановлено ДД.ММ: причина, что нужно», коммит, и следующая по порядку.
   Больше двух задач за сессию не брать.
3. Готово — коммит и пуш в main, раздел (или сделанный шаг) удалить тем же
   коммитом; номер вычеркнуть из порядка в этом промпте.
4. Последнее сообщение владельцу, коротко и простыми словами: что сделано,
   что нужно от него (или «ничего»); затем этот блок из NEXT.md для копирования
   и какая задача следующая; ждёт времени — когда запускать. Порядок пуст —
   «эстафета закончена», без промпта.
```

## 6. Мемы: вид готов, смешно пока через раз

**Почему важно:** мем — главный источник роста, его пересылают чужим. Вид
владелец утвердил 11.09.2026, а юмор нет: первые пачки вышли тухлыми
(итоги года, «слушателей за месяц: 41», цифры вместо шутки). После того как
правила мема переписали вокруг его рефа, из шести мемов понравились три.
Владелец пришлёт ещё рефы — с ними и доводить.

```
Проект «ПЛЁНКА»: папка ПЛЕНКА в домашней директории. Общайся по-русски.

Сначала проверь, влита ли в origin/main ветка post-form (рубрика МЕМ с картинкой:
data/memes.json, assets/meme/templates/, card.render_meme). Если нет — работай
в её рабочей копии .claude/worktrees/post-form, в устаревшее дерево ~/ПЛЕНКА
не пиши: с 12.09.2026 оно выровнено по origin/main, правки там потеряются.

Что уже есть. Мем выходит картинкой: шаблон из каталога data/memes.json
(47 штук, key/name/when/source), надпись на самой картинке сверху и снизу
белым гротеском с тонкой обводкой, водяной знак канала в углу, текст поста —
подписью под фото. Модель отвечает полями top, bottom, text, picture
(src/llm.py, схема мема). Правила — prompts/rubrics/meme.md. Вид утверждён,
его не трогай.

Задача — чтобы мемы были смешными. Материал:
1. Реф владельца: ~/Downloads/photo_2026-09-11 13.19.13.jpeg — 3D-яблоко
   с руками, сверху «Иногда я говорю «хз»», снизу «Но я знаю». Его слова:
   «почти не имеет смысла, но смешнее». Это направление, а не шаблон для всех.
2. Новые рефы — спроси у владельца, куда он их положил. В репозиторий их
   не коммить: это чужие мемы, они нужны только для разбора.
3. Вердикт владельца на пачку по новым правилам. Понравились:
   - енот застрял | Слушаю альбом с середины / Начало я не заслужил |
     подпись: Первый трек включу, когда стану лучше
   - курица из Minecraft | Знаю все слова этой песни / Ни одного правильно |
     подпись: Пятнадцать лет пою свою версию. Оригинал уже звучит как кавер
   - рыба смотрит | Ставлю трек на повтор / Чтобы он ко мне привык |
     подпись: Сегодня он поздоровался первым
   Не то совсем:
   - курица | Слушаю грустное на кухне / Мне норм | Сосед стучит в стену. Тоже, наверное, слушает.
   - енот | Слушаю один трек четвёртый месяц / Остальные берегу | Плейлист на 600 песен. Открываю ради одной
   - рыба | Слушаю один трек сорок минут / Он длится две | На сорок первой минуте начинаю понимать слова
   Гипотеза для проверки: в удачных поворот одушевляет или выворачивает логику
   (начало надо заслужить, трек должен привыкнуть), в неудачных — буквальная
   констатация. Ещё три видимых дефекта: четыре надписи из шести начинаются
   со «Слушаю», картинки в пачке повторяются, в подпись пролезают цифры.

Порядок: разбери рефы и вердикт → сформулируй механику → перепиши meme.md
(удачные мемы — в образцы, неудачные — в антиобразцы) → добавь в каталог
картинки под рефы (только реальные мем-картинки, не генерация нейросетью,
без встроенного текста, без политики и смерти) → добей разнообразие в пачке
(не повторять картинку и первое слово надписи в одном прогоне compose.plan).

Проверка: 6–8 мемов через compose.generate_checked("meme", payload) без записи
в очередь, отправь их владельцу в личку (publish.send на TELEGRAM_ADMIN_ID)
все подряд, без отбора, и спроси вердикт. Повторяй, пока он не скажет «да».
В канал ничего не публикуй, content/queue не трогай, без его согласия не пушь.
```

---

## 21. Релизы из дежурства: проверить первый живой выход

**Почему важно:** 12.09.2026 задача 7 закрыта: ежечасный крон `publish --releases`
убран из publish.yml, релизы выпускает дежурство (`moderate.publish_shift`,
раз в 10 минут рядом с push_state, интервал — `publish.release_due`). Проверено
только самопроверками; в канал по новому пути ещё ничего не выходило.
13.09.2026 проверить было не на чем: с 11.09 11:29 UTC сбор не нашёл ни одного
релиза (выходные), смены шли без строки «Выход релиза». В 12:00 МСК перепроверено:
iTunes и Deezer отвечают по всем 156 артистам без ошибок, свежее 11.09 есть только
чужой сингл City Morgue, и сбор его верно отсеял. Смотреть после первого
нового релиза, обычно в ночь на пятницу. Ближайший известный — Kaito Shoma
«Shadows», в inbox с 13.09 предзаказом, выход 18.09 07:00 UTC: compose --fresh до этого
его верно не берёт.

```
Проект «ПЛЁНКА»: папка ПЛЕНКА в домашней директории. Общайся по-русски.
Работай в свежем клоне origin/main.

1. Журнал дежурства (gh run list --workflow moderate.yml, лог смены): строка
   «Выход релиза: …» появляется, пост уходит по PUBLISH_TARGET, следующий —
   не раньше чем через час (config.RELEASE_EVERY_HOURS).
2. Смена длится шесть часов и держит код с момента старта: пока она идёт,
   правки publish.py до выхода релиза не доходят. Убедись, что это не мешает —
   пуш в src/ ставит свежую смену (moderate.code_changed).
3. Сбор и срочные новости больше не вытесняются: раньше выход в :50 занимал
   группу state-write. Посмотри по gh run list, что collect.yml и urgent.yml
   идут близко к расписанию.
4. Если дежурство почему-то не идёт часами, релизы вообще не выходят —
   сторож src/health.py этого не видит. Стоит ли ему проверять возраст
   последнего выхода релиза, спроси владельца одной строкой.
Проверки: python -m src.publish --selftest, python -m src.moderate --selftest.
```

---

## 24. Чужой голос в посте: проверить вживую и включить отзывы YouTube

**Почему важно:** 12.09.2026 закрыта задача 22 — в данные поста о релизе
приходит поле `outside` (`compose.outside_voice`): мнение издания из новостей
Telegram, а если его нет — отзыв слушателя под клипом на YouTube. Первый путь
проверен на живых данных и работает, второй ждёт ключа: `YOUTUBE_API_KEY` пуст
и в .env, и в секретах Actions. Выяснилось и другое: цитировать дословно умеет
Claude, а GigaChat пересказывает своими словами — тогда чужой голос снимается
с задания, и пост пишется как раньше. В канал по этому пути ещё ничего
не выходило.

```
Проект «ПЛЁНКА»: папка ПЛЕНКА в домашней директории. Общайся по-русски.
Работай в свежем клоне origin/main.

1. Посмотри вышедшие посты о релизах русской сцены: появилась ли в них цитата
   издания и стоит ли рядом имя того, кто это сказал. Если в логах Actions
   на каждом релизе «чужая цитата пересказана своими словами», значит
   на GigaChat правило не работает: скажи об этом владельцу одной строкой
   и предложи отдавать outside только при LLM_PROVIDER=anthropic.
2. Отзывы слушателей: если владелец завёл ключ (Settings → Secrets and
   variables → Actions → YOUTUBE_API_KEY), проверь
   `python -m src.sources.youtube_comments --check "Баста" "Сияй"`.
   Квота 10 000 единиц в сутки, один пост стоит 101 — посмотри по логам,
   сколько уходит за день.
3. Хвост: сухой прогон `compose --fresh --dry-run` с заданным ключом тоже
   тратит квоту — payload собирается до проверки dry_run. Начнёт мешать —
   не зови youtube_comments, когда прогон сухой.
Проверки: python -m src.compose --selftest, python -m src.quality --selftest,
python -m src.sources.youtube_comments --selftest. Ничего не публикуй.
```

---

## 8. Хвосты поста о релизе: длинный текст, ВКонтакте, рамка

**Почему важно:** с 12.09.2026 пост о релизе — одно сообщение (publish.send): обложка
в рамке канала (card.cover), текст и площадки строкой в нём же. Кнопок нет, плеера рядом
нет, полный трек уходит первым комментарием (comments.seed). Осталось несколько мест,
где музыка или рамка молча не доезжают.

```
Проект «ПЛЁНКА»: папка ПЛЕНКА в домашней директории. Общайся по-русски.
Работай в свежем клоне origin/main.

1. ~~Подпись к медиа в Telegram — не больше 1024 знаков.~~ Закрыто 12.09.2026:
   лимит считается по видимому тексту, а не по разметке (telegram.visible_len),
   и строка площадок в него почти не входит. Ужимать промпты не понадобилось.
2. Отрывок магазина в ленту больше не идёт: `compose.fresh_preview`, поле `preview`
   в постах и `--backfill-music` остались от прежнего вида. Проверь, нужен ли отрывок
   кому-то ещё (СЛЕПАЯ ПРОСЛУШКА берёт свои), и что не нужно — убери.
3. ~~Во ВКонтакте музыка не уходит~~ — и не сможет: wall.post отказывает
   и треку, и плейлисту (проверено 12.09.2026). Ссылка на подборку сообщества
   стоит в тексте записи, обложка уходит вложением.
4. Рамку обложки проверили только на обложке iTunes: CDN Deezer из домашней сети
   отвечает 403, и дома card.cover возвращает None — уходит голая обложка по ссылке.
   Посмотри в канале первый релиз с обложкой Deezer и первую срочную новость
   с портретом (urgent.with_portrait): в рамке ли они.
Проверки: python -m src.publish --dry-run, python -m src.vk --check. В канал не публикуй.
```

---

## 13. СЛЕПАЯ ПРОСЛУШКА: первый живой прогон титулов

**Почему важно:** 11.09.2026 (коммиты fee9112, 9d28eb3) викторина переехала
в комментарии: отрывок уходит в канал, неанонимный опрос под пересылкой вешает
дежурство, угадавшие назавтра получают метку «знаток: <артист>» (setChatMemberTag,
Bot API 9.5). Сломается молча: запасной путь отдаст викторину в канал, и титулов
просто не будет. Так и вышло с первой загадкой 12.09 (15:05 UTC): дежурство поймало
пересылку, но `_push_repo` выходил на упавшем `git add quiz.json` — файла у дежурства
ещё не было — и не доходил до pull, загадка к нему не приехала. Починено 13.09.2026:
неудача add больше не отрезает pull, случай добавлен в moderate --selftest. Открытый
data/quiz.json проверен: там только отпечатки.

14.09.2026 проверено: загадка 13.09 (15:42 UTC) ушла «в комментариях», опрос в приватном
quiz.json записан через 9 секунд после отрывка, у бота в чате обсуждений есть
can_manage_tags. Титулы вживую проверить не на ком: в канале 6 подписчиков, у загадки
3 просмотра, в чате обсуждений 2 участника — владелец и бот, а админу метку Telegram
не ставит. Раздача без угадавших теперь пишет в лог «Титулы: угадавших нет».

```
Проект «ПЛЁНКА»: папка ПЛЕНКА в домашней директории. Общайся по-русски.
Работай в чистой копии от origin/main. Прослушка — src/quiz.py (attach, take_answer,
award), голоса ловит src/moderate.py. Загадка, опрос и угадавшие — в приватном
berg-creator/plenka-state (quiz.json и папка quiz/<id опроса>/<id человека>).

Когда в чате обсуждений появятся обычные участники (telegram.member_count на
linked_chat_id канала больше 2) и после загадки в plenka-state появится папка quiz/:
1. Лог следующего запуска quiz.yml: «Титул «знаток: …»: N из M угадавших».
   «Метка не встала» — смотреть текст ошибки Telegram (длина, символы в имени).
   «Титулы: угадавших нет» при живых голосах — не доходят poll_answer.
2. Спросить владельца, видна ли метка у угадавших.
Id людей и их метки в вывод и в открытый репозиторий не печатать. Правки проверять
python -m src.quiz --selftest. Меток реальным людям руками не ставить.
```

---

## 14. Claude Opus 5 вместо GigaChat — отложено владельцем

**Почему важно:** GigaChat выдумывает прошлое артистов: 11.09.2026 у Смоки Мо
«серия больших альбомов» (за лето вышло 4 сингла), у kizaru «первый трек начинается
с фразы, которой закончился прошлый альбом». Claude Opus 5 на тех же данных писал
по треклисту. В коде Claude уже подключён (src/providers/claude.py, claude-opus-5,
кэш голоса, пакет для обычной очереди), в воркфлоу ключ прокинут — не хватает
секрета и переменной. Владелец 11.09.2026 решил пока остаться на GigaChat.

```
Проект «ПЛЁНКА»: папка ПЛЕНКА в домашней директории. Общайся по-русски.
Делай только после «да» владельца на вопрос о цене.

1. Значение ANTHROPIC_API_KEY из ~/ПЛЕНКА/.env → gh secret set ANTHROPIC_API_KEY
   (в вывод не печатать). В collect, urgent, calendar, clips, compose и moderate
   ключ уже прокинут в env.
2. gh variable set LLM_PROVIDER --body anthropic. Запасной остаётся gigachat
   (vars.LLM_FALLBACK || 'gigachat') и поднимается только на технических отказах.
3. Докстринг src/providers/claude.py («нужен, если захочется сравнить качество»)
   поправить: теперь это основной генератор.
4. Проверка: после ближайшего сбора — лог compose --fresh
   (gh run list --workflow collect.yml): пишет Claude. Через сутки —
   расход в console.anthropic.com против оценки, доложить владельцу.

Оценка 11.09.2026: около $0,7 в день, $20 в месяц. Посты о релизах и срочные новости
пишутся сразу, по полной цене; пакетом, вдвое дешевле, идёт только обычная очередь.
В августе ушли на GigaChat, потому что «Claude недоступен в России» (коммит a533a04);
11.09 ключ из .env отвечал.
```

---

## 15. Первый пост о релизе по-новому: проверить в канале

**Почему важно:** 12.09.2026 пост о релизе стал одной плиткой: обложка в рамке, текст,
площадки строкой, ни кнопок, ни плеера. Полный трек уходит первым комментарием.
Проверено в личке и на поддельных апдейтах, в канале — ещё нет.

```
Проект «ПЛЁНКА»: папка ПЛЕНКА в домашней директории. Общайся по-русски.
Работай в свежей копии origin/main. Отправка — src/publish.py (send), первый
комментарий — src/comments.py (seed), зовёт его дежурство src/moderate.py (process).

После первого поста о релизе в канале (gh run list --workflow publish.yml, выход --releases):
1. Страница t.me/s/plenka_fm: пост одной плиткой, в конце текста строка «▸ Слушать — …»,
   под постом видна кнопка «Комментарии».
2. Журнал дежурства (moderate.yml) около времени поста: пришла одна пересылка, под ней
   первым комментарием встал полный трек (или вопрос, если трека к посту не было).
   12.09.2026 это не работало вовсе: send_audio падал с UnboundLocalError
   и ронял смену на каждом посте о релизе (коммит 7e1d32b). Под постом
   «kizaru — CA$HEY» в 12:36 МСК трека так и не появилось — проверь, встал ли
   он после починки, хотя бы задним числом на следующем релизе.
3. Рубрика и трек берутся из data/posted.json и архива, а дежурство подтягивает их
   git-ом прямо перед комментарием (push_state в moderate.process). Проверь по журналу,
   что подтягивание успевает: рубрика вопроса своя, трек тот самый.
getUpdates не вызывать, в канал ничего не публиковать. Правки проверять
python -m src.moderate --selftest.
```

---

## 16. Автопилот точности: первые запуски и хвосты ловли

**Почему важно:** владелец 11.09.2026 требует полный автопилот — посты сам не проверяет.
GigaChat выдумывает, поэтому выдумки ловятся в два слоя: грубо при генерации
(quality.problems, коммит d1d722c) и умно после — облачный Claude по подписке каждые
2 часа (рутина «ПЛЁНКА — автопилот точности», бриф prompts/review.md, правки применяет
review.yml). 11.09 в 21:50 МСК запущен разовый полный проход по очереди.

```
Проект «ПЛЁНКА»: папка ПЛЕНКА в домашней директории. Общайся по-русски.
Работай в свежей копии origin/main.

Проверено 13.09.2026 и из списка убрано: быстрый выход по git diff читает только
посты за 6 часов (проход 08:35 UTC — 4 поста вместо 32), правка вышедшего поста
доходит до канала (12.09 20:41, «не принял канал: 0»), у retry есть actions: write,
сторож пишет о красных запусках остальных воркфлоу (health.failed_runs).

2. Дыры грубой ловли (живой прогон GigaChat 11.09): «третья работа за лето», «ещё один
   короткий сингл», «закрепил тенденцию» проходят; «все треки укладываются в 3 минуты»
   и «меньше трёх минут каждый» с треклистом не сверяются (у kizaru три трека за 3:00).
   Добавлять только с новым замером потерь на тех же 4 заданиях — compose.generate_checked
   на отпечатках inbox 695a898a, 8f033904, e1f700d9 (РЕЛИЗ) и 8f033904 (ВЕРДИКТ).
3. Прощённый брак описи выходит с описью прозой («16 треков», «ЗА 38 МИНУТ» в заголовке):
   вырезается только строка <code>. Автопилот точности это не правит: опись — правда,
   а не выдумка, бриф (§5) лишь запрещает её дописывать. 13.09 в очереди описи
   не было, в канале она осталась у четырёх постов 11.09 — до новых правил.
   Появится снова — ловить в quality.py, а не в брифе.
4. previous_releases сверяет каждую старую строку inbox с магазином — запрос на строку
   при каждом посте. Начнёт тормозить сбор — кешировать.
8. Первый проход 11.09 (коммит 3e8725d) превратил вердикт nkeeei & Yanix в пустышку
   с цитатой «Дослушал до конца. Второй раз не стал.» — это реплика из образца формы
   в voice.md, а модель не слушала. Бриф поправлен (своего не дописывать, пустое снимать),
   пост снят. Реплики «от слушателя» в voice.md («неделю не снимаю с повтора») голос любит,
   а точность — нет: спросить владельца одним вопросом, оставлять ли их.
Проверки: python -m src.quality --selftest, python -m src.compose --selftest,
python -m src.review --selftest. В канал не публиковать, getUpdates не вызывать.
```

---

## 17. Картинка к разборам: хвосты

**Почему важно:** 12.09.2026 разбор уходит в канал кадром — фотография артиста,
найденного в тексте, в рамке канала (`card.cover` → `footage.artist_image`).
Проверено на живой очереди: кадр нашёлся у 12 разборов из 14. Осталось три места,
где картинки по-прежнему нет.

```
Проект «ПЛЁНКА»: папка ПЛЕНКА в домашней директории. Общайся по-русски.
Работай в свежем клоне origin/main.

Проверка везде одна: python -m src.publish --dry-run печатает строку «Картинка»
и строку «Вид» — видно, уйдёт пост кадром или текстом.

1. Два разбора про фонк и Мемфис уходят текстом: в самом тексте нет ни одного
   имени из data/artists.json, там только город и жанр. Имена есть в записи
   data/lineage.json, по которой пост написан (ancestor, facts: «Три 6 Мафия»,
   «Томми Райт Третий»), но пост не хранит, из какой записи он сделан, а имена
   в базе связей записаны по-русски — footage.find_artist ищет по data/artists.json
   и такие не находит. Варианты: класть в пост поле с артистом при генерации
   (src/compose.py, add("lineage", link, {})) или завести в artists.json русские
   написания. Второе отзовётся и в клипах — там та же сверка имени.
2. Разбор длиннее 1024 знаков уходит текстом: подпись к фото у Telegram не длиннее.
   В очереди 12.09 такой был один из 14 (1080 знаков). Это та же беда, что в пункте 1
   задачи 8, — чинить заодно: ужать лимит в prompts/rubrics/*.md.
3. Во ВКонтакте разбор идёт без картинки: vk.post берёт только ссылку на фото
   (_upload_photo), а кадр нарисован у нас файлом. Починка — та же, что вышла
   у card.photo_backdrop: принимать и путь к файлу. Заодно во ВКонтакте появится
   картинка у мема. Проверка: python -m src.vk --check.
```

---

## 18. Разовая дозагрузка треклистов к старым находкам

**Почему важно:** в `data/inbox.jsonl` 104 релиза, из них 30 без треклиста
и 6 с треклистом без отрывков. Посты по ним выходят беднее: писать не о чем,
кроме подписи и даты. Механизм перенесён 12.09.2026, но ни разу не запускался.

```
Проект «ПЛЁНКА»: папка ПЛЕНКА в домашней директории. Общайся по-русски.
Работай в свежем клоне origin/main.

Запусти python -m src.collect --backfill-tracks и закоммить data/inbox.jsonl.
Осторожно: файл переписывается целиком, а в .gitattributes у *.jsonl стоит
merge=union — параллельный автоматический сбор в это время задвоит строки.
Поэтому: сначала git pull, потом запуск, потом сразу коммит и push, и глянь,
не идёт ли в это время collect.yml (gh run list --workflow=collect.yml).
Проверь по паре дозагруженных релизов, что треки и отрывки на месте:
python -m src.compose --fresh --dry-run.
```

---

## 27. Ключ GIPHY: локальный отвечает 401

Всплыло 14.09.2026 на задаче 26, 16.09 ответ тот же. `GIPHY_API_KEY` из ~/ПЛЕНКА/.env на любой запрос
к api.giphy.com (search, trending, stickers) отвечает 401 Unauthorized — ключ
недействителен или не активирован. Секрет Actions обновлён в тот же день в 18:10 UTC,
его значение проверить не удалось: сборка keef3 брала гифки по id, без поиска.
Без рабочего ключа поиск по `query` мёртв, и гифка без `id` (или с удалённым id)
откатывается на шаблон или сток. Нужен владелец: только он может выпустить ключ.

Промпт:

```
Проект «ПЛЁНКА», ~/ПЛЕНКА, по-русски. Проверь ключ GIPHY: без печати значения
сделай curl -s -o /dev/null -w "%{http_code}" "https://api.giphy.com/v1/gifs/search?q=cat&limit=1&api_key=$KEY"
с ключом из ~/ПЛЕНКА/.env. 200 — вычеркни задачу. 401 — попроси владельца одной
фразой без жаргона: зайти на developers.giphy.com, создать ключ типа API и прислать
его; положи в .env и в секрет Actions GIPHY_API_KEY (gh secret set -R berg-creator/plenka),
проверь тем же curl. Затем сборку ролика, где у gif нет id, — в логе reels.yml должно
быть «найден поиском», а не «GIPHY ответил 401».
```

## 31. Оживление фото артиста без ИИ-лица

Владелец 15.09.2026 предложил вместо фото артиста в роликах генерировать
ИИ-анимацию с ним. Реалистичную ИИ-анимацию лица реального человека не делаем:
YouTube требует помечать правдоподобно изменённый контент с реальными людьми,
TikTok сам метит ИИ-контент, а канал уже сносили с YouTube в январе 2026-го
за синтетическое лицо (CLAUDE.md, «Лицо канала — плёнка»). Вместо этого —
2.5D-параллакс по карте глубины: фигура и фон двигаются с разной скоростью,
лицо не меняется. Применять к кадрам `face` и `photo` и к фото из `pics/`.
Главный вопрос — реально ли это в CI без новой зависимости в requirements.txt.

Промпт:

```
Проект «ПЛЁНКА», ~/ПЛЕНКА, по-русски. Прочитай CLAUDE.md, раздел 31 в NEXT.md,
в src/clips.py — segment (наезд zoompan), в src/reels.py — build, STILL_ZOOM
и --selftest. Работай в чистой копии от origin/main; приватное хранилище —
клон berg-creator/plenka-state, STATE_DIR на него.

1. Проверь осуществимость без новой зависимости в requirements.txt: модель
   глубины в onnx (Depth Anything V2 small, MiDaS small — лицензии проверить,
   нужна разрешающая коммерческое использование), запуск onnxruntime+numpy,
   которые ставятся в reels.yml отдельным шагом только для сборки роликов
   (pip install в воркфлоу, не в requirements.txt), модель скачивается и кэшируется
   actions/cache. Замерь время на одну картинку на ubuntu-latest (локально —
   порядок величины). Больше ~10 с на кадр или лицензия не годится — стоп.
2. Годится: функция в src/reels.py, которая по картинке и карте глубины делает
   клип 9:16 с параллаксом (сдвиг слоёв по глубине 2–4% ширины, края заливать
   растяжкой, без разрывов у контура головы), вместо простого наезда
   для kind face/photo и jpg из pics. Модели нет или шаг упал — обычный наезд,
   сборка не падает.
3. --selftest: на синтетической картинке «ближний квадрат на дальнем фоне»
   с заданной картой глубины первый и последний кадр различаются, ближний
   слой сдвинулся больше дальнего. Без сети — если модели нет, тест
   параллакса пропускается с сообщением, а откат на наезд проверяется.
4. Пересобери локально keef3 (python -m src.reels --build 20260914-keef3
   --dry-run), лист кадров раз в 0,5 с — глазами: лица не плывут, у контуров
   нет разрывов. Хуже простого наезда — не включать, сказать.
5. Итог коротко владельцу через основную сессию: включили или нет и почему.
   Коммит и пуш в main, раздел удалить тем же коммитом. Не выходит в CI без
   зависимостей — записать в «Закрытые вопросы» одной строкой и удалить раздел.
```

## 32. Разбор удержания роликов

Первый ролик keef3 (36 с, выложен 15.09.2026 вечером): в TikTok досмотрели до конца 4,4 %,
среднее время просмотра — 7 с; в YouTube Shorts «продолжили смотреть» 68 % — лучшее из площадок. По разбору 15.09.2026 в `prompts/reels.md` появился
раздел «Удержание» (15–25 с, удар в первые 2 с, сильнейший кадр первым, вопрос
для спора в конце). Проверить его можно только цифрами: владелец присылает график
удержания через 48 часов после каждого из первых пяти роликов (YouTube Studio →
ролик → Аналитика → Удержание аудитории; VK Клипы → статистика клипа). Скриншоты
приходят в основную сессию или боту. Задача живёт, пока не разобраны пять роликов.

Промпт:

```
Проект «ПЛЁНКА», ~/ПЛЕНКА, по-русски. Прочитай CLAUDE.md, раздел 32 в NEXT.md,
prompts/reels.md (раздел «Удержание» и «Форматы по дням») и prompts/reels-edit.md.
Работай в чистой копии от origin/main; приватное хранилище — клон
berg-creator/plenka-state, STATE_DIR на него. Секреты — ~/ПЛЕНКА/.env, значения не печатать.

1. Владелец прислал график удержания ролика <id> (скриншот YouTube Studio или VK
   Клипов). Выпиши точки: где кривая падает круче всего (секунда и процент),
   процент на 3-й секунде, досмотр до конца, среднее время просмотра.
2. Собери ролик локально: STATE_DIR=… python -m src.reels --build <id> --dry-run.
   Лист кадров каждые 0,5 с (ffmpeg -i reel.mp4 -vf fps=2 f-%03d.png, склейка
   сеткой) и время фраз (субтитры в логе сборки, дубли в plenka-state/reels/<id>/).
   Сопоставь каждую точку ухода с кадром и фразой: что было на экране и что
   звучало за секунду до падения (мем с текстом, медленная фраза, пауза, проходной
   кадр, концовка-плашка).
3. Одна-две причины, повторяющиеся от ролика к ролику, — правка в prompts/reels.md
   (раздел «Удержание» — коротко, с цифрой из графика). Разовая причина — строкой
   в таблицу ниже, в бриф не нести. Правило в брифе, которое цифры опровергли, — убрать.
4. Допиши в этот раздел строку: id, досмотр, среднее, 3-я секунда, главная точка
   ухода и её причина. После пятого ролика — итог в одну фразу владельцу через
   основную сессию и раздел удалить.
5. Коммит и пуш в main (git pull --rebase перед пушем).
```

Разобрано: пока ни одного (keef3 до графика: TikTok — досмотр 4,4 %, среднее 7 с;
YouTube Shorts — «продолжили смотреть» 68 %, средний просмотр 0:21 из 0:36, со слов владельца
16.09.2026). Площадки мерить
раздельно: TikTok и Shorts дают разные цифры, общий вывод из одной — ошибка.

## 33. Первые ежедневные сценарии по новому брифу

С 15.09.2026 рутина «ПЛЁНКА — сценарий ролика» (trig_01PfYM8L5Wx89i6KWsDXSdbn)
запускается каждый день в 06:00 UTC и берёт формат дня из раздела «Форматы по дням»
в `prompts/reels.md`: Пн, Ср, Вс — НОВОСТЬ, Вт — ОТКУДА ЗВУК, Чт — КАК X СТАЛ X,
Пт — УГАДАЙ ТРЕК, Сб — ТРИ ТРЕКА ИЗ БОТА (меньше трёх — ИСТОРИЯ или ОТКУДА СЭМПЛ). Там же новые правила удержания,
поле `comment`, обязательная приманка со ссылкой на бота и строка `track` (отрывок
превью iTunes). 17.09 работает и разовая рутина про L'One
(trig_018qEAH84nSvxZzF8JLvxEut) — в её промпте устаревшее «кадры stock и card»,
бриф главнее. 14.09 плановый запуск пропал: у облачной среды был закрыт интернет
(см. память reels-decisions).

Промпт:

```
Проект «ПЛЁНКА», ~/ПЛЕНКА, по-русски. Прочитай CLAUDE.md, раздел 33 в NEXT.md,
prompts/reels.md целиком, prompts/reels-edit.md, в src/reels.py — problems,
_excerpt, _under_tracks, build. Работай в чистой копии от origin/main; приватное
хранилище — клон berg-creator/plenka-state, STATE_DIR на него.

1. За 16, 17 и 18.09: пришли ли сценарии. RemoteTrigger list_runs по
   trig_01PfYM8L5Wx89i6KWsDXSdbn и trig_018qEAH84nSvxZzF8JLvxEut, у каждого
   запуска get_run_log; ветки claude/reels-* и запуски reels.yml
   (gh run list -R berg-creator/plenka -w reels.yml). Не пришёл — причина из лога
   (сеть среды, --check, push); чинится промптом рутины или брифом — чини,
   сеть среды — одной фразой владельцу, что включить на claude.ai/code.
2. Каждый сценарий (plenka-state/reels/<id>/script.json) против брифа: формат
   совпадает с днём недели по Москве (или это сильная новость вне очереди);
   5–7 фраз, 40–60 слов; первая фраза до 10 слов и слово-крючок в первой
   половине; первый кадр — сильный визуал, мем с текстом не в первые 10 с;
   comment есть и это вопрос для спора; в description приманка со ссылкой
   на бота, ссылка на канал, 3–5 хэштегов; title начинается с имени героя;
   у каждого факта два источника. Расхождения, повторяющиеся в двух сценариях, —
   правка брифа, а не сценария.
3. Пятница 18.09, УГАДАЙ ТРЕК: есть строки track (id, start, length ≤ 10),
   в логе сборки reels.yml нет «Отрывок трека … не нашёлся»; в собранном ролике
   отрывки звучат в своё время, бит под ними почти молчит (ebur128 или
   volumedetect по участкам), голос и субтитры на них не лезут.
4. Первый ролик по новому брифу, у которого есть все дубли: собрать локально
   (STATE_DIR=… python -m src.reels --build <id> --dry-run) и посмотреть лист
   кадров каждые 0,5 с по prompts/reels-edit.md, пункт 5. Длина 15–25 с,
   на концовке основной версии под @plenka_fm строка «кидай трек — покажем»,
   TikTok-версия кончается последней строкой, без плашки.
5. Итог коротко в основную сессию; найденное чинить сразу, --selftest reels
   зелёный, коммит и пуш в main (git pull --rebase перед пушем), раздел удалить
   тем же коммитом.
```

## 39. Тестовый рекламный пост на 2–3 тысячи — после СВЕДЕНИЯ

Бюджет на 16.09.2026 — 10 000 ₽. Порядок трат — «Фаза 3» в `GROWTH.md`: сначала
тест в одном канале, рекламируем функцию, а не канал (поправка 18.09.2026 там же).
Отбор для рекламы отвергнут. Делать, когда построено СВЕДЕНИЕ (задача 50) и его
ролик прошёл порог спроса; не прошёл — деньги не трогать.

Промпт:

```
Проект «ПЛЁНКА», ~/ПЛЕНКА, по-русски. Прочитай CLAUDE.md, «Третий актив»
и «Фаза 3» в GROWTH.md, раздел 39 в NEXT.md.

1. Подбери 5 каналов на 1–5 тысяч подписчиков про музыку (не только рэп:
   сравнить музыку с другом хочет любой, кто слушает Яндекс),
   где публично видны цена поста, охват и ERR (каталоги telega.in, tgstat).
   Живые, без накрутки: охват поста не меньше 15% подписчиков, комментарии
   живыми людьми. Таблица: канал, подписчики, охват, цена, почему он.
2. Три варианта рекламного текста, каждый — про СВЕДЕНИЕ, а не про канал
   («кинь боту ссылку — узнай, на сколько процентов у вас с другом совпадает
   музыка»), ссылка
   t.me/plenka_fm_bot?start=ad_<канал>.
3. Итог владельцу в основную сессию: один канал и один текст на первый тест,
   сумма, и как читать результат: через 48 часов — приходы по метке
   (--sources), сравнения и приведённые ими друзья, прирост подписчиков канала; через неделю —
   сколько осталось. Цена подписчика = сумма / число.
4. После теста владелец присылает, сколько заплатил. Цифры — строкой в этот
   раздел; сошлось (подписчик дешевле 80 ₽) — предложить разложить остаток
   на 3–5 каналов, не сошлось — сказать, что чинить, и деньги не трогать.
   Раздел удалить, когда решение по остатку принято.
```

## 40. Рассылка по чатам артистов — когда ролики набирают

Делать, когда три ролика подряд на YouTube набрали от 1000 просмотров
(владелец присылает цифры). До того рассылку не трогать: в канале единицы
подписчиков, пост в нём артисту ничего не даёт, а заход в чат разовый —
раздел «Как зовём в бота» в GROWTH.md, решение 16.09.2026.

Промпт:

```
Проект «ПЛЁНКА», ~/ПЛЕНКА, по-русски. Прочитай в GROWTH.md раздел «Как зовём
в бота» и раздел 40 в NEXT.md. Работай в чистой копии от origin/main.

1. Владелец прислал просмотры последних роликов по площадкам. Подставь в текст
   рассылки честное число вместо N — наименьшее из трёх последних на YouTube,
   округлённое вниз. Число подписчиков канала — с t.me/plenka_fm; перевалило
   за 300 — фразу «новый канал» убрать.
2. Перепроверь чаты из таблицы: живы ли и сколько участников (страница
   t.me/<чат>, строка members). Мёртвые убери, число обнови.
3. Проверь, что суббота «ТРИ ТРЕКА ИЗ БОТА» в prompts/reels.md на месте:
   текст обещает ролик.
4. Готовый текст и список — владельцу в последнем сообщении, простыми словами.
   Рассылает сам. Коммит и пуш, раздел 40 удалить тем же коммитом.
```

## 42. Приходы из роликов за концертами и релизами — замерить, прежде чем строить что-то ещё

С 17.09.2026 ролик раскрывает тему через боль и кончается решением — ботом
(prompts/reels.md, «Боль и решение»). Ссылки `t.me/plenka_fm_bot?start=gorod_<артист>`
(концерты в городе) и `?start=slezhu_<артист>` (релизы) подписывают на артиста,
концовка ролика — по ссылке (src/reels.py, `bait`; src/service.py, ветка `gorod_`/`slezhu_`). Сценарий
Басты 17.09 с такой ссылкой владелец отверг — герой не на пике, записан не был.
Слежение с того же дня требует подписки на канал, как отбор и разборы. Делать, когда вышли 2–3 ролика с такими ссылками.

```
Проект «ПЛЁНКА», ~/ПЛЕНКА, по-русски. Работай в чистой копии от origin/main.
Приватное хранилище — клон berg-creator/plenka-state, STATE_DIR на него.

1. STATE_DIR=… python -m src.service --sources — сколько пришло по меткам «gorod»,
   «slezhu» и «yt/tt/vk» за те же дни; в watches.json (не печатать id) — сколько
   подписчиков слежения и у скольких есть город. Просмотры роликов — у владельца.
2. Приходов по «gorod» и «slezhu» нет при тысяче просмотров и больше — ничего
   под концерты и релизы не строить, раздел удалить; спросить владельца, что
   в роликах за боль, раз на решение не идут. Есть — тогда, и только
   тогда, думать о посте канала про гастроли, с условиями из «Закрытых
   вопросов» («Рубрика в канале под рекламу бота»).
3. Итог владельцу одной строкой с числами. Раздел удалить тем же коммитом.
```

## 44. Трек с YouTube: первый настоящий релиз

С 17.09.2026 запрос трека несёт ссылку на видео той же длины, что в магазине
(src/tracks.py, `find`), а помощник на Mac владельца (launchd `fm.plenka.tracks`)
качает его и отвечает боту на запрос. Проверено без сети и живым поиском, но не
на настоящем посте: у постов, написанных до этого, нет поля `seconds`.
Уже сделано 17.09 в 17:30: поиск с GitHub находит 10 из 10 (запуск probe-ytdlp),
ключ входа Mac `TELEGRAM_SESSION_MAC` лежит в `.env` и действует, Mac качает
без deno. Первый пост с `seconds` ждать не раньше ночного сбора 18.09
(в пятницу релизы выходят в полночь), Kaito Shoma «Shadows» — 18.09 в 10:00 МСК.

```
Проект «ПЛЁНКА», ~/ПЛЕНКА, по-русски. Работай в чистой копии от origin/main.

1. Возьми первый пост о релизе, написанный после 17.09.2026 17:30 МСК
   (content/archive или content/queue): есть ли `seconds` и
   `track_request.youtube`. Нет `youtube` при `seconds` — в логе collect.yml/urgent.yml
   шага ask-tracks найди, что ответил поиск (YouTube мог закрыть поиск и серверу).
2. На Mac: tail ~/Library/Logs/plenka-tracks.log — ушёл ли трек («✓») или
   почему нет; у поста в архиве должен появиться `full_track_file_id`.
   Сам не запускай src.tracks без --dry-run: трек уйдёт боту от имени владельца.
3. Сломалось — чини, --selftest tracks и compose зелёные. Итог одной строкой
   владельцу, раздел удалить тем же коммитом.
```

## 45. Трек к посту, который уже вышел

17.09.2026 владелец попросил трек под последний пост канала (Travis Scott «RHYNO»,
№144): пост писали руками, запроса трека не было. Сделано разово: видео Atlantic
Records (штрихкод релиза 075679… — её), скачано на Mac, бот ответил плеером
на пересылку поста в чате обсуждений (сообщение 235) с подписью прежнего вопроса,
а сам вопрос удалил; `full_track_file_id` записан в архив. Сейчас так не умеет
никто: `moderate.attach_track` на пост из архива отвечает «уже вышел», а
`tracks.pending` смотрит только очередь. Строить — только если попросит ещё раз.

```
Проект «ПЛЁНКА», ~/ПЛЕНКА, по-русски. Работай в чистой копии от origin/main.
Трек, пришедший после выхода поста, должен встать под пост, а не пропасть.
1. src/moderate.py attach_track: пост из архива с `message` — не отказ, а плеер
   ответом на пересылку поста в чате обсуждений. Её номер боту не известен:
   пересылку ловит comments.seed — запоминать там её message_id в архив поста.
   Первый комментарий-вопрос без трека бот удаляет, подпись плеера — тот же вопрос.
2. src/tracks.py pending: брать и архив за последние сутки, где есть
   track_request.youtube и нет full_track_file_id.
3. Самопроверки moderate, tracks, compose зелёные; раздел удалить тем же коммитом.
```

## 47. ПРОПАЖА: что исчезло из плейлиста — три проверки, потом стройка

Остановлено 18.09.2026: владелец счёл идею слабой. Задача 49 нашла сильнее —
СВЕДЕНИЕ (раздел 50): ПРОПАЖА остаётся в нём одной кнопкой, отдельно не строить.
Раздел удалить, когда раздел 50 построен или отвергнут.

Предложение и причины — «ПРОПАЖА» в `GROWTH.md` («Третий актив»), 18.09.2026.
Требование владельца: функция полезна при любом числе подписчиков, рекламируют
её, а не канал. Делать, когда владелец сказал «строим» и ответил, играет ли у него
в Яндекс Музыке без VPN альбом «Россия34» Славы КПСС.

```
Проект «ПЛЁНКА», ~/ПЛЕНКА, по-русски. Работай в чистой копии от origin/main.
Прочитай «ПРОПАЖА» в GROWTH.md и раздел 47 в NEXT.md; отчёты исследования —
docs/research/2026-09-17/, читать только если упрёшься.

Сначала три проверки, без кода бота. Любая провалена — строка «Остановлено
ДД.ММ: что именно» в этот раздел, коммит, итог владельцу, дальше не идти.
1. Страна запроса. api.music.yandex.net/users/<логин>/playlists/<номер> без ключа
   отдаёт плейлист с available и error у трека (проверено 18.09 с адреса в США).
   Запусти тот же запрос из Actions (разовый workflow_dispatch или шаг в health):
   отвечает ли, остаются ли скрытые треки в списке. Блокировки «только для
   России» оттуда не видны: «Россия34» из США доступна. Ответ владельца про этот
   альбом решает: у него не играет — первая версия честно говорит только о треках,
   снятых совсем (error no-rights), и это написано в ответе бота; ключ его
   аккаунта пробуем позже отдельной задачей.
2. Масштаб. Десять больших публичных плейлистов (поиск api…/search?type=playlist,
   логины не печатать и не сохранять): в скольких есть скрытые треки и сколько их.
   Пусто в восьми из десяти — не строим.
3. Ссылки. Какие адреса плейлиста отдаёт приложение сейчас (users/<логин>/
   playlists/<номер>, playlists/<uuid>, короткие) и читается ли каждый без ключа;
   что видно у закрытого профиля.

Стройка — по образцу src/otbor.py и слежения в src/service.py:
- src/sources/yandex_music.py: плейлист по ссылке → название и треки (id, артисты,
  название, available, error); --check "ссылка".
- src/propazha.py: приём ссылки в личке (дежурство, src/moderate.py; ссылка
  на плейлист узнаётся раньше otbor.subject), подписка на канал, ответ шаблоном
  без модели: сколько треков, какие скрыты — поимённо, у каждого поиск на других
  площадках (как publish.listen); длинный список — первые 30 и счёт остальных.
  Кнопка «🔔 Следить за артистами плейлиста» — самые частые имена в service.watch_add
  до WATCH_LIMIT. Снимок (id → «Артист — Трек», что уже названо пропавшим) —
  в config.PRIVATE, до трёх плейлистов на человека; логин Яндекса — данные
  о человеке, в открытый репозиторий не попадает.
- Раз в сутки, следом за вестями о концертах в collect.yml: перечитать плейлисты,
  написать о новых пропажах, одна весть на трек. Трека нет в списке вовсе — человек
  убрал его сам, молчим.
- Бот говорит только то, что в данных: трек и «Яндекс больше не даёт его слушать».
  Причину («закон», «цензура») не называть — в данных её нет.
- Метка ?start=propazha в service.SOURCES; команда /propazha в меню (src/setup.py);
  health.py замечает, что Яндекс перестал отвечать без входа.
- --selftest и --dry-run, докстринг по принятому формату, строки в CLAUDE.md
  (команды, архитектура) и README.md, галочка в чек-листе GROWTH.md.

Закончить: коммит и пуш в main, раздел удалить тем же коммитом. Владельцу —
простыми словами: что бот теперь умеет, что он честно не видит, и что пятую
боль можно добавлять в ролики (задача 48, набросок про SODA LUV).
```

## 48. Три ролика, где бот — развязка: полные сценарии по наброскам

Наброски — «Как зовём в бота» в `GROWTH.md`, 18.09.2026. Итог 19.09.2026:
- **Kanye West → Markul.** Афиша Канье не отдаёт, поэтому герой с живым туром:
  Markul 16.09 перенёс Insomnia Tour на весну, Москву и Питер добавят позже.
  Сценарий `20260919-markul` ушёл в ветку `claude/reels-20260919-markul`,
  развязка — `?start=gorod_markul`; Markul для этого внесён в `data/artists.json`.
- **kizaru снят**: факт наброска не подтвердился. Альбом, который он «готовил
  3 года» и обещал на май 2025-го, — SIMBA, вышел 20.06.2025 (The Flow 10.04.2025,
  hiphop.ru, kanobu.ru); Ca$hey (11.09.2026) — другой альбом. Новый ролик о нём —
  только с новой свежей новостью.
- **SODA LUV** ждёт СВЕДЕНИЯ с кнопкой пропаж (задача 50): звать в функцию,
  которой нет, нельзя.

```
Проект «ПЛЁНКА», ~/ПЛЕНКА, по-русски. Работай в чистой копии от origin/main.
Прочитай раздел 48 в NEXT.md.

Делать с 26.09.2026 (неделя после выхода ролика Markul): дату выхода и просмотры
спросить у владельца одной строкой, если их нет здесь.
STATE_DIR=<клон plenka-state> python -m src.service --sources — приходы
по gorod_markul строкой в раздел 42. Ролик ещё не вышел — строка «Остановлено».
SODA LUV — когда задача 50 построена: сценарий по наброску 1 в GROWTH.md
(«Как зовём в бота»), пятая боль в «Боль и решение» (prompts/reels.md), приманка
propazha в src/reels.py рядом с gorod_ и slezhu_, python -m src.reels --check,
ветка claude/reels-<id>. Раздел 48 удалить, когда вышел SODA LUV и замер Markul
записан — или владелец отверг набросок.
```

## 50. СВЕДЕНИЕ: «на сколько ты такой-то артист» — сначала функция, потом ролик

Решение владельца 18.09.2026: **ролик о функции, которой ещё нет, не снимаем.**
Проверка спроса роликом отменена: ролик `20260918-sved` не записывается (его фразы
уже в боте, их просто не озвучивают), а смешная сценка, придуманная вместо него,
выходит один раз, и бот к этому моменту должен уже считать. Спрос и так виден:
18 266 голосов с 01.10.2019 у просьбы «найти людей с тем же вкусом» — самая большая
тема форума Яндекс Музыки (`docs/research/2026-09-18/06-feature-votes.md`).

**Упор — на сравнение себя с артистом** (владелец: «это самое ценное»). Оно работает
для одного человека, без друга: кинул ссылку на свои «Мне нравится» — сразу ответ.
Поэтому строится первым, а сравнение двоих — вторым шагом. Бот на `?start=sved_*`
и на ссылку плейлиста (`service.SVED_LINK`) пока честно отвечает «делаем» и копит ждущих
в `config.SVED_FILE` — им напишет стройка.

Проверено 18.09.2026 с Mac владельца: `api.music.yandex.net` отвечает без ключа —
`search?type=artist&text=…` и `artists/<id>/brief-info` (поля `similarArtists`,
`playlists`, `playlistIds`). У любого артиста в `playlists` есть «Лучшее: X» и «В стиле: X» —
редакционные, владельцы uid 457553308 и 460142547; у некоторых свой плейлист
(«Баста респектует», uid 2231978693). Лайки самого артиста закрыты, как у всех. Поэтому
«вкус артиста» честно — это сам артист и его круг (`similarArtists`, «В стиле: X»),
а его личный плейлист — только где он правда есть. Голосом и в боте — «совпадаешь
с Токсисом», а не «залезли в его лайки».

Итоги проверок 50а (19.09.2026):
- **С GitHub Actions Яндекс не отвечает**: search, brief-info, likes/tracks, playlists/3
  и tracks — HTTP 451 (brief-info — 403), серия из 20 запросов — ни одного ответа.
  С Mac владельца (Россия) все те же запросы идут без ключа. Нужен российский адрес:
  прокси или VPS, решает владелец.
- Лайки открытого аккаунта читаются: users/music-blog/likes/tracks — 207 треков
  (id и albumId), названия и исполнители — tracks?track-ids=…, поле available есть.
- Приватность: «Публичный доступ к моей фонотеке» — на сайте, music.yandex.ru/settings/other;
  в приложении такой настройки нет. Новый плейлист со смартфона приватен, пока им
  не поделились (yandex.ru/support/music/ru/collection/playlists). Открыто ли «Мне нравится»
  у нового аккаунта по умолчанию, не подтверждено — 50б отвечает на закрытый список
  ссылкой на эту настройку.
- Конкурента нет: каталоги ботов и подборки 2025 года сравнения вкуса по Яндекс Музыке
  не знают. «Моя волна на двоих» — с 22.07.2026 (yandex.ru/company/news/22-07-2026-03),
  только Плюс, общий поток рекомендаций на двоих; процента, общих артистов и сравнения
  с артистом в ней нет. Не пишем «у Яндекса такого нет» — пишем, что умеем мы.

Порядок: 50а → 50б → 50г → 50д; 50в и 50е — позже, 50ж ждёт владельца.

### 50а. Российский адрес для запросов к Яндексу — ждёт владельца

Остановлено 19.09.2026: с серверов GitHub Яндекс отвечает 451, нужен адрес в России.
Нужно от владельца одно из двух: купить прокси с российским адресом (HTTP, 100–300 ₽
в месяц) и положить его строкой `http://логин:пароль@адрес:порт` в GitHub → Settings →
Secrets → Actions как `YANDEX_PROXY` — или VPS в России за 200–300 ₽ в месяц и доступ к нему.
Помощник на Mac не годится: Mac спит, а на ролик люди приходят в любой час.

```
Проект «ПЛЁНКА», ~/ПЛЕНКА, по-русски. Работай в чистой копии от origin/main.
Прочитай раздел 50 в NEXT.md (шапку и 50а).

Секрет YANDEX_PROXY заведён (или у владельца VPS — поднять на нём HTTP-прокси
только для api.music.yandex.net и завести секрет самому). Разовый воркфлоу
workflow_dispatch: запросы search?type=artist, artists/<id>/brief-info,
users/music-blog/likes/tracks, tracks?track-ids=… через прокси (requests, proxies=)
отвечают — стройка 50б пойдёт через него; воркфлоу удалить. Не отвечают — строка сюда.
Сколько артистов из data/artists.json находится точным именем, у скольких similarArtists
не пуст и у скольких свой, не редакционный плейлист — посчитать там же, с паузой
в секунду и проверкой, что ответ пришёл (прогон 18.09 без пауз дал ложный ноль).
Итог строкой в шапку раздела 50, 50а удалить, коммит и пуш в main.
```

### 50б. Стройка: «ты на N% такой-то» — делать после 50а

```
Проект «ПЛЁНКА», ~/ПЛЕНКА, по-русски. Работай в чистой копии от origin/main.
Прочитай раздел 50 в NEXT.md (шапку и 50б), src/otbor.py (образец отдельной функции
бота) и слежение в src/service.py (watch_add, WATCH_LIMIT).

- src/sources/yandex_music.py: «Мне нравится» и плейлист по ссылке → треки (id, артисты,
  название, available); артист по имени → id, similarArtists, свои плейлисты;
  --check "ссылка" и --artist "Имя". Все запросы — через YANDEX_PROXY (50а): с GitHub
  Яндекс отвечает 451; без секрета — напрямую, как с Mac. Закрытый список — ответ
  со ссылкой music.yandex.ru/settings/other («Публичный доступ к моей фонотеке»).
- src/svedenie.py: ссылку принимает дежурство (src/moderate.py; service.SVED_LINK
  уже узнаёт её раньше otbor.subject — заглушку service.sved_wait заменить приёмом),
  за подписку на канал. Ответ шаблоном без модели: три артиста, с которыми человек
  совпадает сильнее всего, — «ты на N% <артист>»; кнопка «Сравнить с артистом» —
  человек пишет имя, бот отвечает процентом. Формула — в докстринге: доля доступных
  треков из «Мне нравится», где хоть один исполнитель — сам артист или его круг
  (similarArtists из brief-info). Есть у артиста свой плейлист — второй строкой
  «с тем, что слушает он сам: N%» (общие треки к меньшему из двух списков).
  Кнопка «🔔 Следить за ним» — service.watch_add.
- Картинка-карточка с процентом для сторис (src/card.py): крупно «N% <артист>»,
  аватар канала, адрес бота. Без неё результатом не делятся.
- Ждущим из config.SVED_FILE — одно сообщение: «заработало, кидай ссылку», файл
  удалить. service.SVED — про артиста, а не только про друга.
- Логин Яндекса и списки — данные о человеке: только config.PRIVATE, до трёх
  разборов в сутки на человека, снимок списка не дольше 30 дней.
- Бот говорит только то, что в данных: процент и имена. Вкус не оценивает.
- Команда в меню (src/setup.py); health.py замечает, что Яндекс перестал отвечать
  без входа; --selftest и --dry-run; докстринг по принятому формату; строки
  в CLAUDE.md, README.md и команды проверки в CLAUDE.md; пятая боль в prompts/reels.md
  («Боль и решение»: «на сколько ты <артист>» — «кинь лайки — посчитаем»),
  reels.BAIT_SVED под неё.

Закончить: коммит и пуш в main, 50б удалить тем же коммитом. Владельцу — пусть
кинет боту свою ссылку и пришлёт, что ответил бот (его процент пойдёт в сценку 50г).
```

### 50в. Сравнение двоих — делать после 50д

```
Проект «ПЛЁНКА», ~/ПЛЕНКА, по-русски. Работай в чистой копии от origin/main.
Прочитай докстринг src/svedenie.py и раздел 50 в NEXT.md (50в).

Личная ссылка ?start=sv_<код>: второй человек кидает свою — обоим ответ шаблоном:
процент (общие артисты и треки к меньшему из двух списков — формулу в докстринг),
общие артисты, первые 30 общих треков, карточка «N% совпали» для сторис. Кнопки:
«Что Яндекс убрал из моего плейлиста» (бывшая ПРОПАЖА, раздел 47: available=false,
поимённо, причину не называть) и «Дубли». До трёх сравнений в сутки на человека.
--selftest, строки в CLAUDE.md и README.md. Коммит и пуш в main, 50в удалить.
```

### 50г. Ролик-сценка «свидание» — делать, когда 50б в канале

Набросок владельца 18.09.2026: парень смотрит на девушку — идеальная; она даёт ему
наушник, а там артист, которого он считает кринжем, и подпевает всерьёз; он в абсурдной
ярости; а потом бот показывает, что он сам совпадает с этим артистом на 89%.
Юмор — главное: рассмешил — значит, к тому, кто зовёт в бот, уже дружеское отношение.

```
Проект «ПЛЁНКА», ~/ПЛЕНКА, по-русски. Работай в чистой копии от origin/main.
Прочитай раздел 50 в NEXT.md (50г) и prompts/reels.md целиком.

Сценарий по наброску владельца, голосом владельца, кадры — сток, гифки, мемы.
Черновик 18.09.2026 (доводить по «Удержанию» и «Формуле»):
  1. Она идеальная. Ровно до того, как дала мне наушник.
  2. Там Токсис. Не в шутку. Она подпевает.
  3. Я молча переворачиваю стол. Потом соседний. Потом весь зал.
     (пауза, гифка с переворотом стола)
  4. А потом ПЛЁНКА сравнила мои лайки. Я — Токсис на восемьдесят девять процентов.
  5. Кинь ПЛЁНКЕ ссылку на свои лайки — посчитаем, чей ты.
- Ярость — на предметы, не на девушку: насилие к женщине площадки режут даже
  в шутку, и в брифе это запрещено. Мата нет.
- Сценка — вымысел, фактов в ней нет; но что бот умеет — только правда: процент
  с артистом и его кругом. Процент — настоящий ответ бота на лайки владельца,
  если он его прислал.
- Артист-«кринж» — узнаваемое имя (Токсис из наброска владельца — проверить, что
  он на Яндексе и у него есть similarArtists); шутка над публичным человеком, без злобы.
- title — «Она дала мне наушник — и свидание закончилось», comment — «Какой артист
  в наушниках — сразу красный флаг?», в description ссылка ?start=sved_yt и строка
  про 18 тысяч голосов на форуме Яндекса.
Проверка: python -m src.reels --check. Пушем в ветку claude/reels-<дата>-svidanie,
как велит бриф; ветку claude/reels-20260918-sved, если висит, удалить.
Коммит и пуш, 50г удалить тем же коммитом.
```

### 50д. Замер сценки — делать, когда она висит на площадках 3 дня

```
Проект «ПЛЁНКА», ~/ПЛЕНКА, по-русски. Работай в чистой копии от origin/main.
Прочитай раздел 50 в NEXT.md (50д и 50ж).

Дату выхода и просмотры Shorts, TikTok, VK спросить у владельца одной строкой,
если их нет в разделе. STATE_DIR=<клон plenka-state> python -m src.service --sources —
приходы по меткам sved, sved_yt, sved_tt, sved_vk, sved_link с дня выхода.
Смешная сценка соберёт и тех, кто пришёл только посмеяться, поэтому смотреть и долю,
и штуки: от 5 приходов на тысячу просмотров (все площадки вместе) или вдвое больше
приходов, чем по gorod_ и slezhu_ (раздел 42), — сценка работает, 50ж владельцу
на решение; меньше — второй ролик с другим заходом (спор друзей, у кого вкус хуже),
строка сюда. Итог строкой в шапку раздела 50, 50д удалить, коммит и пуш.
```

### 50е. Запуск без денег — делать после 50в

```
Проект «ПЛЁНКА», ~/ПЛЕНКА, по-русски. Работай в чистой копии от origin/main.
Прочитай раздел 50 в NEXT.md (50е).

Готовые тексты — владельцу в личку, отправляет он сам со своих аккаунтов, один раз:
ответ в теме yandexmusic.userecho.ru/communities/45/topics/421 («есть бот, который это
делает», без рекламы канала), ответы под 3–5 свежими вопросами о сравнении вкусов
на Ответах Mail.ru, пост на Пикабу «сделал бота, который считает, на сколько ты
совпадаешь с артистом». У каждой ссылки своя метка ?start=sved_<площадка>
(добавить в service.SOURCES). Коммит и пуш, 50е удалить.
```

### 50ж. Блогер снимает сценку вживую — ждёт владельца

Идея владельца 18.09.2026: не обычная реклама, а блогер записывает сценку со свиданием
по нашему сценарию как свой обычный ролик — в ресторане, с живой девушкой; живые
актёры абсурд играют смешнее, чем голос поверх гифок. Только когда бот считает (50б)
и сценка голосом владельца прошла замер (50д): деньги на рекламу — только на СВЕДЕНИЕ
и только после ролика, прошедшего порог (GROWTH.md, «Поправка 18.09.2026»). Тогда
сессия подбирает 3–5 блогеров со скетчами (не музыкальные каналы), бриф со сценарием,
ссылка ?start=ad_<блогер>; решает и платит владелец.

## Закрытые вопросы — не переоткрывать

- **Накрутку подписчиков с бирж заданий не покупаем.** 18.09.2026 Gemini
  советовал 100–150 «живых на вид» подписчиков по 1–3 ₽ (Unu, Taskpay, Seosprint).
  Они канал не читают, охват на подписчика падает, биржи рекламы метят такой
  канал накрученным, а замер ERR перед тратами («Фаза 3» в GROWTH.md) теряет
  смысл. Человеку из ролика нужна польза, а не счётчик.

- **Рубрика в канале под рекламу бота (концерты, отбор) не строится.** 17.09.2026
  три независимых предложения («КУДА ЕДЕТ», «ПО ГОРОДАМ», «ГАСТРОЛИ» — пост
  о туре по Яндекс Афише со строкой «свой город — боту») разбились об одно и то же,
  проверено вживую: Афиша показывает только 10 ближайших дат, поэтому «тур
  по 9 городам» и «объявил» — выдумка; у распавшихся групп на странице трибьюты
  (у «Сектора Газа» все 10 дат — «посвящение Юрию Хою»); Скриптонита и Моргенштерна
  Афиша не отдаёт; читателей у поста 6, и в бота из него никто не придёт. Концерты —
  единственное в боте, что полезно с первого дня и не зависит от подписчиков,
  и ровно поэтому их место в ролике для чужой аудитории, а не в посте канала:
  приманка по герою (prompts/reels.md). Под отбор рубрика тоже не нужна: его
  реклама — сам первый пост отбора, а первую заявку при шести подписчиках дают
  ролики и знакомые владельца, а не канал. Возврат к рубрике — только по живым
  приходам по метке «gorod» (задача 42), и тогда с условиями: первый проход
  Афиши лишь запоминает даты, без «объявил» и «N городов», белый список живых
  гастролёров, выход мимо слотов (`publish.due`), один запрос в день на артиста,
  общий с `notify_concerts`.

- **Мешап и генерация песни в боте отвергнуты.** 16.09.2026 владелец предложил
  «диджея»: склеить голос одного трека с битом другого или сгенерировать новую
  песню в двух жанрах. Автоматический мешап звучит через раз, у слушателей нет
  файлов, а склейка чужих треков — повод закрыть бота. Suno и ElevenLabs после
  исков лейблов не пропускают чужое аудио, так что генерация идёт только
  по словам и с присланными песнями не связана; к тому же сгенерированная
  музыка спорит с правилом «ничего не выдумывать».
- **Распознавание трека («что играет в видео») не делаем.** 16.09.2026 владелец:
  «шазам проще будет».

- **Вопроса читателю в посте нет, а в комментарии бота — есть.** 12.09.2026
  владелец забраковал концовку «а ты когда последний раз дослушивал русский
  альбом до конца?»: раскачка на комментарий видна насквозь. Запрет — во всех
  рубриках, ловит `quality.problems`; 13 постов очереди, написанных по старым
  правилам, почищены (коммит b492dbb). Реплику бота под постом это не касается.

- **Реплики бота в комментариях пишутся по-чатовски** — с маленькой буквы,
  не всегда со знаком в конце («кто уже включил — как оно?»). 12.09.2026
  попытку причесать их до нормальных предложений владелец откатил: это
  намеренный тон, а не неряшливость.

- **Описи нет ни строкой, ни прозой, ни в одном предложении.** 12.09.2026
  забракованы «Девять вещей, 23 минуты» (SODA LUV) и «один трек в релизе: 2:51,
  и это всё» (OG BUDA). Ловится так: счёт вещей и одно время в предложении —
  брак, два времени — наблюдение («первые шесть уложены между 2:04 и 2:20»),
  оно законно.

- **Цитата в посте не загадка и не шутка про постороннее.** «Ни одной чужой
  фамилии» пришлось объяснять (это про фиты), «Лучше бы сделали сериал» —
  не о релизе. Правило — в `prompts/voice.md`, пункт про цитату.

- **Канал не смотрит на артиста свысока.** «Ни один трек не дотянул до трёх
  с половиной минут — короткие истории, которые легко повторить дважды»
  (kizaru, 12.09.2026) — снисходительность, да ещё о музыке, которую никто
  не слушал. Проверил по архиву: из 19 постов о русских релизах разнос был
  в 6, но три последних подряд вышли колкими. Стёб — по сцене и индустрии.

- **Бот в личке остаётся с разбором строк из песен (/tekst), лимит — один разбор
  в сутки.** 12.09.2026 владелец выбрал этот вариант из трёх: потерянная ветка
  `sync-clean-2026-09-11` (коммит a85ce31) ставила главным слежение за релизами,
  убирала разбор текстов и поднимала лимит до трёх. Ветку в этой части не переносим,
  она больше ни для чего не нужна.

- **Музыку во ВКонтакте не приложить.** 12.09.2026 проверено на отложенной записи:
  `wall.post` от ключа сообщества отказывает и треку, и плейлисту, с фото и без —
  «Required a combination of photo attachments for audio attachment». Подборка
  сообщества уходит ссылкой в тексте, вложением идёт только обложка. Второй ключ
  от личного аккаунта заводить не будем — он живёт недолго и его надо продлевать.

- **Кнопок под постом нет вовсе, площадки — ссылками в тексте.** У поста канала
  с инлайн-кнопками Telegram не показывает комментарии — это ожидаемое поведение
  (bugs.telegram.org/c/41803). Панель из восьми кнопок к тому же читалась в ленте
  раньше самого поста. Владелец 12.09.2026: пожертвовать кнопками, но сделать пост
  красивее; страницу-прокладку выбора площадок (song.link) отвергли — в ней нет
  ни VK Музыки, ни Звука, и она уводит из Telegram.
- **Плеера рядом с постом нет, полный трек — первым комментарием.** Вид ленты не должен
  зависеть от того, прислали трек к релизу или нет (владелец 12.09.2026: «плеер ни к селу
  ни к городу»). Отрывок магазина в ленту не идёт совсем.
- **Описи в РЕЛИЗЕ и ВЕРДИКТЕ нет — ни строкой `<code>`, ни предложением.** Владелец
  сам вырезал её из поста Ghost Mountain 11.09.2026, а 12.09.2026 забраковал и её
  развёрнутый вид («самый долгий трек 3:07, самый короткий — 1:38»): цифры без вывода
  растягивают пост. Пересказ описи
  quality.problems сверяет с данными релиза; упрямый пост выходит без описи,
  а не пропадает.
- **Посты о свежих релизах и срочные новости пакетом не пишем.** Пакет вдвое дешевле,
  но ответ приходит через минуты, а бывает и часы. Владелец 11.09.2026: срочное
  выходит вовремя, а Claude без пакета (~$0,7 в день) пока дорого — остаёмся на GigaChat.
- **Дешёвые модели Claude ради экономии не берём.** 11.09.2026 на 4 постах (3 РЕЛИЗА
  и ВЕРДИКТ): Opus 5 — 7 запросов за $0,30, писал по данным; Sonnet 5 — 8 запросов
  за $0,18, выдумал Смоки Мо «Ю.Г. в 90-х»; Haiku 4.5 — 10 запросов за $0,055,
  потерял 3 поста из 4. Модель дешевле — посты хуже.
  Открытый репозиторий удешевляет только минуты Actions, токены — нет.
- **Опрос прослушки живёт в комментариях, а титул знатока выдаётся назавтра.**
  В канале голоса анонимны — бот не узнает, кто угадал. Метка, поставленная
  сразу, спалила бы ответ всем, кто ещё листает комментарии. Решено с владельцем
  11.09.2026, подробности — в докстринге src/quiz.py.
- **Полный трек берётся с YouTube: сервер ищет, качает Mac владельца.** 17.09.2026
  владелец вернулся к автоскачиванию: он и сам брал трек с YouTube через сайт.
  С сервера GitHub поиск нашёл 10 релизов из 10 с длиной как в магазине, скачивание —
  0 из 3 («Sign in to confirm you're not a bot»), из дома скачалось 3 из 3.
  Поэтому `compose --ask-tracks` кладёт в запрос ссылку на видео, а src/tracks.py
  на Mac (launchd, свой ключ входа TELEGRAM_SESSION_MAC) качает и шлёт боту
  ответом на запрос. Обход защиты YouTube на сервере не пробуем: проверка
  безопасности остановила его как атаку на чужой сервис. Отвергнуты и бот-качалка
  по промпту Gemini (перезаписал бы .env и requirements.txt, второй поллер
  на токене канала), и SoundCloud (совпало 3 из 10, и то перезаливы).
  Ниже — как было до этого решения.
- **(До 17.09.2026) Полный трек бот сам не ищет — запрос уходит владельцу сразу.** Стриминги
  отдают 30 секунд, остальное — перезаливка без прав. Полный трек присылает
  владелец (задача 9).
  Легальные бесплатные источники проверены 11.09.2026 на 13 реальных постах
  и 10 артистах: 0 попаданий.
  - Bandcamp отдаёт бесплатно только релизы 3–9-летней давности. Тёзки опасны:
    sematary.bandcamp.com — группа из Новой Зеландии.
  - На SoundCloud у официальных треков скачивание выключено, скачать можно лишь
    перезаливы. Ключ API требует подписки Artist Pro.
  - Наших артистов нет в Jamendo и FMA, у FMA API мёртв.
  - Audius проверен 10.09.2026: там перезаливы («ghostemane-mercury_(Devmp3indir.Net)»
    от аккаунта с нулём подписчиков), не оригиналы.
  Бесплатное скачивание к тому же не даёт права выкладывать трек в канал.
  Владелец закрыл тему 11.09.2026. Ссылки на файлообменники и конвертеры бот
  тоже не подбирает — под запросом только магазин и поиск YouTube, SoundCloud, Bandcamp.
- **Логотипы стримингов на кнопках под постом в канале не ставятся.** Иконку
  на кнопке (icon_custom_emoji_id, Bot API 9.4) Telegram показывает в каналах
  только ботам с купленным на Fragment именем; Premium владельца открывает её лишь
  в личке и группах. Проверено 11.09.2026 по документации. Поэтому кнопки подписаны
  словами, по три в ряд: при четырёх подписи обрезались у всех, кроме VK.
- **song.link (Odesli) для выбора стриминга не годится.** 11.09.2026: публичный API
  отвечает 401 PUBLIC_API_ACCESS_DEPRECATED, а на странице выбора нет ни Spotify,
  ни Яндекса, ни VK — даже у ASTROWORLD. Кнопки собирает publish.listen: у магазина,
  откуда пришла находка, точная ссылка на релиз, у остальных — поиск.
- **RSS по русской рэп-сцене вымер.** 10.09.2026 проверено около 90 адресов, причины
  стоят у отключённых лент в data/feeds.json. Нужен другой механизм (задача 4).

