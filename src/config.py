"""Единая точка конфигурации: пути, рубрики, секреты, расписание."""

from __future__ import annotations

import os
from dataclasses import dataclass
from pathlib import Path

ROOT = Path(__file__).resolve().parent.parent

DATA = ROOT / "data"
# Адрес канала для концовок роликов и надписей: одно место на весь проект.
CHANNEL_HANDLE = "@plenka_fm"
# Бот канала (getMe, 15.09.2026): приманка роликов — разбор вкуса в нём, ссылка в описании.
BOT_HANDLE = "@plenka_fm_bot"
PROMPTS = ROOT / "prompts"
QUEUE = ROOT / "content" / "queue"
# Срочные новости живут отдельно от очереди: их пишут и показывают в тот же
# день, и в общую ленту по расписанию они попадать не должны — пока пост
# дождётся своей очереди, новость успеет протухнуть.
URGENT = ROOT / "content" / "urgent"
ARCHIVE = ROOT / "content" / "archive"
TEMPLATES = ROOT / "assets" / "templates"

ARTISTS_FILE = DATA / "artists.json"
CANDIDATES_FILE = DATA / "artists_candidates.json"
LINEAGE_FILE = DATA / "lineage.json"
SUBTEXT_FILE = DATA / "subtext.json"
FACTS_FILE = DATA / "facts.json"
CALENDAR_FILE = DATA / "calendar.json"
INBOX_FILE = DATA / "inbox.jsonl"
SEEN_FILE = DATA / "seen.json"
POSTED_FILE = DATA / "posted.json"
STORIES_FILE = DATA / "stories.json"
# Журнал обращений к генератору: кто писал — основной или запасной.
# Нужен сторожу: переход на запасной виден только строкой в логе запуска,
# а канал ради ухода с ГигаЧата и переводили (src/health.py).
LLM_LOG = DATA / "llm.jsonl"
# Выгрузка плейлиста ВКонтакте: «артист|трек» → вложение audio-…_…
# Собирается разово руками, потому что audio.search сообществам закрыт.
CLIPS_FILE = DATA / "clips.json"
# Мемные картинки рубрики МЕМ: каталог с подсказкой, когда какая уместна,
# и сами шаблоны. Шаблоны лежат в репозитории, а не качаются при публикации:
# иначе выход поста зависел бы от чужого сайта.
MEMES_FILE = DATA / "memes.json"
MEME_TEMPLATES = ROOT / "assets" / "meme" / "templates"

# Всё, что относится к конкретным людям, лежит отдельно от кода.
# Репозиторий проекта открытый, и списки тех, кто писал боту и за кем следит,
# в нём быть не должны — ни в каком виде. Путь задаётся переменной STATE_DIR:
# в GitHub Actions туда клонируется приватный репозиторий, локально —
# просто папка рядом, которую не берёт git (см. .gitignore).
PRIVATE = Path(os.environ.get("STATE_DIR", "")) if os.environ.get("STATE_DIR") else DATA / "private"
WATCH_FILE = PRIVATE / "watches.json"
# ОТБОР (src/otbor.py): заявки с chat_id артистов и посты, ждущие выхода, —
# до выхода имя и трек в открытый репозиторий не попадают.
OTBOR_FILE = PRIVATE / "otbor.json"
OTBOR_POSTS = PRIVATE / "otbor"
# ДВОЙНИК (src/svedenie.py). Кто пришёл по ?start=sved_ до стройки — тем бот обещал
# написать первым; список одноразовый, рассылка его удаляет. Снимок фонотеки —
# чужие лайки, то есть данные о человеке: тоже только в приватном хранилище.
SVED_FILE = PRIVATE / "sved.json"
SVED_STATE = PRIVATE / "svedenie.json"
# Разборов в сутки на человека: каждый стоит до 25 запросов к Яндексу.
SVED_PER_DAY = 3
# СКЛЕЙКА (src/skleyka.py): заявки с chat_id и номерами сообщений с дорожками —
# чужой неизданный трек, поэтому только здесь. Склейка — пара минут машины
# дежурства, на это время очередь стоит: две в сутки на человека, пересборки
# кнопками — сверх них, но не больше SKLEYKA_TWEAKS на трек.
SKLEYKA_FILE = PRIVATE / "skleyka.json"
SKLEYKA_PER_DAY = 2
SKLEYKA_TWEAKS = 3
SKLEYKA_MAX_MB = 200
# Выход из лимита — только тому, кто в него упёрся. Бонус за артиста, который пришёл
# по личной ссылке и склеил свой трек, живёт SKLEYKA_BONUS_DAYS дней, а сверх лимита
# суток бонусных не больше SKLEYKA_BONUS_MAX: склеивает машина дежурства, не сервер.
SKLEYKA_BONUS_DAYS = 30
SKLEYKA_BONUS_MAX = 3
# Звёзды Telegram: цены — здесь. За звёзды — только больше склеек, всё остальное есть и так.
SKLEYKA_STARS_PACK = 30      # SKLEYKA_PACK склеек сверх лимита на сутки
SKLEYKA_PACK = 3
SKLEYKA_STARS_MONTH = 150    # 30 дней по SKLEYKA_MONTH_PER_DAY склеек в сутки
SKLEYKA_MONTH_PER_DAY = 10

# Какой генератор текстов используется, задаётся в .env переменной LLM_PROVIDER
# (gemini — Google, по умолчанию; anthropic — платный Claude; gigachat — Сбер).
# Переменная LLM_FALLBACK задаёт запасной генератор на случай, когда основной
# недоступен: см. src/llm.py. Сама модель выбирается внутри провайдера
# в src/providers/.

# Сколько постов держим в очереди. Если меньше MIN — health.yml поднимает тревогу.
QUEUE_TARGET = 24
QUEUE_MIN = 6

# Срочные новости: сколько пишем за один заход и через сколько часов
# непринятый пост считается протухшим и удаляется сам.
# Один инфоповод за заход: заходов три в день, и при трёх новостях за раз
# лента получала бы девять срочных сообщений в сутки поверх очереди
# и викторины. Срочное перестаёт читаться срочным, когда его много.
URGENT_PER_RUN = 1
URGENT_TTL_HOURS = 48
# Новость старше этого срока срочной уже не считается.
URGENT_MAX_AGE_HOURS = 36
# Заход срочного — один в день, в 14 МСК (владелец 22.09.2026: трёх новостей
# в сутки поверх остального подписчик не успевает прочесть). Новость живёт
# URGENT_MAX_AGE_HOURS, поэтому вечерняя дождётся завтрашнего захода, а выйдет
# одна, самая весомая. Запускает заход дежурство
# (urgent.shift) раз в 10 минут, а не крон: GitHub 12–16.09.2026 не создал
# ни одного утреннего запуска urgent.yml, остальные опаздывали до трёх часов,
# и новость Pitchfork про артистов GTA 6 пролежала в inbox 17 часов.
URGENT_HOURS_MSK = (14,)
# Окно сбора RSS — от него же, но с запасом: сбор ходит раз в 6 часов, и новость,
# вышедшая сразу после прогона, должна дожить до следующего. Пока окно сбора было
# уже (30 часов), верхняя половина срока срочности пустовала: новость возрастом
# 30-36 часов не попадала в inbox вообще.
NEWS_MAX_AGE_HOURS = URGENT_MAX_AGE_HOURS + 12

# Свежесть релизов — решения владельца от 11.09.2026. От выхода релиза до поста
# не больше суток: старше RELEASE_MAX_AGE_HOURS пост не пишется, а готовый
# убирается из очереди. Пишет его каждый сбор (compose --fresh), выходит он своим
# выходом раз в час (publish --releases), а не в обычные слоты, и ждёт
# полный трек от владельца RELEASE_TRACK_WAIT_HOURS. За московские сутки —
# не больше RELEASE_PER_DAY постов о релизах, самые весомые по score: остальные
# протухают через сутки. Каждый релиз занимает обычный слот (publish.due).
# Владелец 22.09.2026: в пятницу 18.09 вышло 14 постов, прочесть столько нельзя.
RELEASE_MAX_AGE_HOURS = 24
RELEASE_TRACK_WAIT_HOURS = 2
RELEASE_PER_DAY = 3
# Чаще раза в час посты о релизах в ленту не идут: выход считает интервал
# по журналу публикаций (publish.release_due), а не по расписанию GitHub.
RELEASE_EVERY_HOURS = 1

# Тихие часы по Москве — решение владельца от 11.09.2026. Выходы релизов идут
# круглосуточно: ночью пост выходит вовремя, но без звука. Молчит любой пост,
# не только о релизе: крон GitHub опаздывает на часы и может увести вечерний
# слот за полночь.
QUIET_FROM_HOUR, QUIET_TO_HOUR = 23, 9

# Минимальный интервал между публикациями. Публикатор смотрит не на часы,
# а на «сколько прошло с прошлого поста» — так пропуск cron не ломает ленту.
PUBLISH_INTERVAL_HOURS = 3

# Обычные посты очереди — по московским часам: утро и вечер. Вместе со срочной
# новостью в 14 это три поста в обычный день (владелец 22.09.2026: семь в сутки
# подписчик не успевает прочесть). Выпускает их дежурство (moderate.publish_shift) раз
# в 10 минут, а не крон: 13.09.2026 GitHub создавал из четырёх запусков publish.yml
# один-два и с опозданием на 3–5 часов, и в воскресенье без релизов вышел один пост
# из четырёх. Пропущенный час наверстывается, пока не наступили тихие часы.
PUBLISH_HOURS_MSK = (10, 19)

# Сколько дней храним отпечатки в seen.json, чтобы файл не рос бесконечно.
SEEN_TTL_DAYS = 120

# Открывать ли ветку комментариев первым сообщением — см. src/comments.py.
# Пустая ветка под каждым постом читается как «здесь никого нет», поэтому
# канал задаёт вопрос сам. Токенов это не стоит: вопросы готовые.
COMMENT_SEED = True

# Кнопки стримингов под постом о релизе (см. publish.listen): ими публикатор
# заменяет строку «▸ Слушать в …», которая вела в один магазин, а стриминг
# у каждого свой. Точную ссылку на релиз без ключей даёт лишь магазин, откуда
# пришла находка: у сервиса с тем же адресом кнопка ведёт прямо на релиз,
# у остальных — на поиск «артист + название». Страницу выбора song.link
# не взяли: их API закрыт, а на странице нет ни Spotify, ни Яндекса, ни VK.
# Логотипы на кнопках канал поставить не может: иконку (icon_custom_emoji_id)
# Telegram даёт в каналах только ботам с купленным на Fragment именем.
LISTEN_SERVICES: tuple[tuple[str, str], ...] = (
    ("Яндекс", "https://music.yandex.ru/search?text={q}"),
    ("VK", "https://vk.com/search/music?q={q}"),
    ("Звук", "https://zvuk.com/search?query={q}"),
    ("Spotify", "https://open.spotify.com/search/{q}"),
    ("Apple", "https://music.apple.com/search?term={q}"),
    ("YouTube", "https://music.youtube.com/search?q={q}"),
    ("SoundCloud", "https://soundcloud.com/search?q={q}"),
    ("Deezer", "https://www.deezer.com/search/{q}"),
)

# Лимиты бота-сервиса (src/service.py). Разбор стоит примерно столько же токенов,
# сколько пост канала, а бесплатный миллион уже наполовину расписан очередью,
# поэтому потолок нужен с самого начала — накрутить его всегда успеется.
SERVICE_DAILY_USER = 1  # разборов на человека в сутки
SERVICE_DAILY_TOTAL = 40  # разборов на всех в сутки
SERVICE_PER_RUN = 6  # сколько разборов делаем за один запуск поллера

# ОТБОР берёт артистов, которых ещё не знают: с этого числа фанатов на Deezer
# артист считается известным. Deezer в России слушают мало, поэтому числа
# у всех скромные — замерено 16.09.2026: Kizaru 123 тыс., SODA LUV 45 тыс.,
# Kaito Shoma 6 тыс., Молчат Дома 5,6 тыс., 9mice 2,2 тыс., Yung Trappa 2 тыс.,
# Heronwater 1,7 тыс.; у андеграунда с поиска «фонк demo» — от 0 до 135.
# Порог в 1000 отсекает всю сцену, которую знают, и оставляет тех, у кого
# слушатели — друзья и знакомые.
OTBOR_MAX_FANS = 1000


@dataclass(frozen=True)
class Rubric:
    """Описание рубрики: как часто выходит и с каким весом попадает в очередь."""

    key: str
    title: str
    # Доля рубрики в очереди. Сумма весов нормируется, точных чисел не требуется.
    # Перекос в сторону МЕМА сделан намеренно: канал растёт пересылками, а
    # пересылают шутку, а не разбор. Разборы остаются, но перестают быть половиной ленты.
    # Ноль — рубрика в очередь наперёд не пишется: её посты делает отдельный
    # запуск ко дню публикации (ЛЕГЕНДА — календарь, ИНФОПОВОД — src/urgent.py).
    weight: int
    # Из какого сырья строится: release | news | lineage | calendar | verdict | meme | poll | digest
    feeds_on: str
    description: str


RUBRICS: tuple[Rubric, ...] = (
    Rubric(
        key="lineage",
        title="ОТКУДА НОГИ",
        weight=22,
        feeds_on="lineage",
        description=(
            "Ниточка от современного трека к его предку: фонк → Three 6 Mafia, "
            "эмо-рэп → грандж, дрейн → витч-хаус. Главная рубрика канала."
        ),
    ),
    Rubric(
        key="verdict",
        title="ВЕРДИКТ",
        weight=16,
        feeds_on="verdict",
        description=(
            "Реакция на свежий трек или тренд: либо разнос, либо честный респект. "
            "Без вежливой середины — середина не репостится."
        ),
    ),
    Rubric(
        key="news",
        title="ИНФОПОВОД",
        weight=0,
        feeds_on="news",
        description=(
            "Что происходит на сцене: бифы, скандалы, уходы с лейблов, воссоединения, "
            "суды, цифры стримов. Мировая и русская сцена наравне. В очередь не идёт: "
            "новость к своей очереди протухает, поэтому её пишет src/urgent.py "
            "в день события и сразу показывает владельцу."
        ),
    ),
    Rubric(
        key="meme",
        title="МЕМ",
        # На паузе с 12.09.2026 по решению владельца: мемы в канал пока не идут.
        # Вернуть вес 30 — и рубрика снова начнёт попадать в очередь.
        weight=0,
        feeds_on="meme",
        description=(
            "Юмор про музыку и слушателя: ру-рэп, продюсеры, лейблы, стриминг, "
            "плейлисты, тикток, жанровые стереотипы. Самая пересылаемая рубрика — "
            "поэтому и самая весомая: разборы держат лицо канала, а растит его тот, "
            "кого пересылают друзьям."
        ),
    ),
    Rubric(
        key="subtext",
        title="МЕЖДУ СТРОК",
        weight=12,
        feeds_on="subtext",
        description=(
            "Разбор текста: отсылки, двойные смыслы, контекст. Работает на курируемой "
            "базе data/subtext.json — цитаты берутся только оттуда, чтобы канал "
            "никогда не приписал артисту выдуманную строчку."
        ),
    ),
    Rubric(
        key="release",
        title="РЕЛИЗ",
        weight=14,
        feeds_on="release",
        description="Вышло что-то у трекаемого артиста — короткий пост со ссылками.",
    ),
    Rubric(
        key="legend",
        title="ЛЕГЕНДА",
        weight=0,
        feeds_on="calendar",
        description=(
            "Годовщина: смерть, рождение, выход культового альбома. "
            "Пишется в день даты — см. src/calendar_check.py."
        ),
    ),
    Rubric(
        key="poll",
        title="ОПРОС",
        weight=6,
        feeds_on="poll",
        description="Нативный опрос Telegram: кто круче, какой альбом лучше.",
    ),
    Rubric(
        key="otbor",
        title="ОТБОР",
        weight=0,
        feeds_on="otbor",
        description=(
            "Трек, который прислал в бота сам артист, ещё не известный. Пишется "
            "шаблоном в момент выхода, раз в день — см. src/otbor.py."
        ),
    ),
)

RUBRIC_BY_KEY = {r.key: r for r in RUBRICS}

# Рубрики поста о конкретном релизе. На релиз пишется один пост из двух, жребием
# по весам выше (compose.release_jobs), и выходит он своим выходом, а не в очередь.
RELEASE_RUBRICS = ("release", "verdict")


def secret(name: str, *, required: bool = True) -> str:
    """Достаёт секрет из окружения. В GitHub Actions они приходят из Secrets."""
    value = os.environ.get(name, "").strip()
    if not value and required:
        raise RuntimeError(
            f"Не задана переменная окружения {name}. "
            f"Локально — положи её в .env, на GitHub — в Settings → Secrets → Actions."
        )
    return value


def load_dotenv(path: Path | None = None) -> None:
    """Простейший загрузчик .env для локального запуска (в Actions не нужен)."""
    env_path = path or (ROOT / ".env")
    if not env_path.exists():
        return
    for raw_line in env_path.read_text(encoding="utf-8").splitlines():
        line = raw_line.strip()
        if not line or line.startswith("#") or "=" not in line:
            continue
        key, _, value = line.partition("=")
        os.environ.setdefault(key.strip(), value.strip())
