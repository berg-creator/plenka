"""Отбраковка постов перед попаданием в очередь.

Модель работает нестабильно: иногда возвращает служебный JSON вместо текста,
иногда скатывается в школьное сочинение с выводом в конце. Такое лучше поймать
автоматически и перегенерировать, чем показывать читателям.

Проверки намеренно грубые: цель — отсечь явный брак, а не оценивать стиль.

Отдельно ловится выдуманное в РЕЛИЗЕ и ВЕРДИКТЕ: звучание и прошлое артиста,
которых во входных данных нет. Это список корней с живых постов, а не вторая
модель-проверщик: та стоит денег, а выдумку своего же рода охотно одобрит.

    python -m src.quality --selftest   опись, выдуманные звук и прошлое ловятся, законное проходит
"""

from __future__ import annotations

import re

# Обороты, которые прямо запрещены голосом канала. Их наличие означает,
# что модель сползла в интонацию школьного реферата.
BANNED_PHRASES = (
    "резюме:",
    "в итоге получилось",
    "формула успеха",
    "вот так рождается",
    "история циклична",
    "давайте разберёмся",
    "давайте разберемся",
    "стоит отметить",
    "как известно",
    "теперь смотри внимательно",
    "подводя итог",
    "таким образом",
    "так что когда ты",
    "вспомни, что",
    "вот это поворот",
    "тут твоё место",
    "тут твое место",
    "не пропустите",
    "приятного прослушивания",
    "уважаемые подписчики",
)

# Слова-пустышки: одно-два простительно, но если их много — это вода.
# Вторая половина списка — то, чем обычно подменяют разговор о звуке:
# эти слова выглядят как описание музыки, но не сообщают о ней ничего.
FILLER_WORDS = (
    "культовый",
    "легендарный",
    "знаковый",
    "поистине",
    "атмосфера мрака",
    "атмосферн",
    "энергетик",
    "энергичн",
    "мураш",
    "уникальное звучание",
    "ни на кого не похож",
    "глубокий смысл",
    "вайб",
    # Из того же списка в voice.md. Их тут не хватало, и посты вроде
    # «круто качают и цепляют до мурашек» проходили фильтр насквозь.
    # Корни, а не словарные формы: «мурашк» не совпадает с «мурашек»,
    # и на этом ловля срывалась.
    "качает",
    "качают",
    "залета",
)

# У этих рубрик во входных данных нет никакого адреса: разбор связи и цитаты
# берутся из курируемых файлов, шутка — ниоткуда. Ссылка в таком посте всегда
# выдумана моделью и ведёт в никуда — проверено, 404.
RUBRICS_WITHOUT_LINK = frozenset({"lineage", "subtext", "meme", "poll"})

MIN_LENGTH = 60
MAX_LENGTH = 1500

# Рубрики, где пост открывается заголовком. У мема и опроса его нет,
# у разборов бота (service.py) — своя форма.
HEADLINED = ("release", "verdict", "news", "lineage", "subtext", "legend")

# Ссылка вырезается перед сверкой с данными релиза: в адресе бывают цифры.
_LINK = re.compile(r"<a\b.*?</a>", re.IGNORECASE | re.DOTALL)

# Брак описи — длинно, но не враньё: исчерпав попытки, compose.generate_checked
# выпускает такой пост, а не теряет его.
INVENTORY_ISSUES = ("опись <code>", "пересказ описи")

# Звучание, которого нет в данных: релиз модель не слушала, а эти слова
# описывают то, что слышно только ушами. Корни собраны с живых постов
# от 11.09.2026 — «жёсткий ритм, отрывистый вокал», «звук плотный, рваный»,
# «чистый раппинг поверх сырого бита», «всю энергию в одну трешовую минуту».
# Границы слов обязательны: «бит» не должен ловиться в «обитает», «звук» —
# в «звукорежиссёр». «Звучит» не ловится намеренно: «название звучит как
# диагноз» — оборот речи, а не звук. Брак звучания — не опись: после всех
# попыток такой пост пропадает, врать хуже, чем промолчать.
INVENTED_SOUND = re.compile(
    r"\b(?:бит(?:ы|а|у|ом|ов|е|ами|ах)?|ритм\w*|вокал\w*|отрывист\w*|грув\w*|раппинг\w*|флоу|"
    r"звук(?:а|у|ом|е|и|ов|ами|ах)?|звучани\w*|рван(?:ый|ая|ое|ые|ого|ой|ым|ыми|ую|ом)|"
    r"шипя\w*|шипени\w*|грязн\w*|энерги\w*|тр[еэ]шов\w*|с[еэ]мпл\w*|синтезатор\w*|"
    r"бас(?:ы|ов|ом|ами)?|гитар\w*|мелоди\w*|припев\w*|куплет\w*|голос(?:а|ом|е|у)?|"
    # Тексты песен модель не видела так же, как не слышала звук: «тексты про
    # бандитизм» у kizaru (11.09.2026). «Минусов» без «к» — это недостатки, их не трогаем.
    r"минусовк\w*|минусовок|текст(?:ы|ов|ах|ами)|строч(?:к\w*|ек))\b"
)

# Номер релиза: «третий сингл», «8-й альбом».
_ORDINAL = (
    r"(?:\d+-?(?:й|ий|ый|ой|го|ого|м|ым)|перв(?:ый|ого|ом|ым|ая|ую)|втор(?:ой|ого|ом|ым|ая|ую)|"
    r"трет(?:ий|ьего|ьем|ьим|ья|ью)|(?:четв[её]рт|пят|шест|седьм|восьм|девят|десят)(?:ый|ой|ого|ом|ым|ая|ую))"
)

# Прошлое, которого данные не подтверждают никогда: inbox помнит только то,
# что канал нашёл с августа 2026, и нашёл не всё. Поэтому ни паузу, ни
# возвращение, ни «первый за год», ни номер релиза из него не вывести:
# «третий сингл за лето» у Смоки Мо врал и с данными — синглов было четыре,
# а совместные с Vito «Долги» сверка с магазином в историю не пускает.
# Живое 11.09.2026: «после долгого молчания», «после серии успешных альбомов»,
# «уже третий сингл подряд», «вернулся к корням», «начинал ещё с «Ю.Г.» в 90-х»,
# «молчал полгода», «третья часть одноимённой трилогии». «До него за лето
# вышли X и Y» сюда не попадает.
INVENTED_PAST = re.compile(
    r"\b(?:молчани\w*|молчал\w*|замолчал\w*|затишь\w*|вернул\w*|возвращени\w*|камб[еэ]к\w*|"
    r"к\s+корням|дебют\w*|успешн\w*|начинал\w*|трилоги\w*|после\s+серии|"
    r"после\s+(?:\w+\s+)?(?:перерыв|пауз)\w*|впервые\s+за|"
    r"перв\w+\s+(?:\w+\s+)?за\s+(?:\d+\s+)?(?:год|лет|месяц|полгода|недел|осен|зим|весн|сезон)\w*|"
    r"(?:сингл|релиз|альбом)\w*\s+подряд)\b"
)

# Номер релиза — отдельно и со своей причиной для повтора. С общей («о прошлом
# только по previous_releases») GigaChat трижды подряд писал Смоки Мо «третий
# сингл за лето»: принимал счёт по полю за данные, и пост пропадал.
RELEASE_NUMBER = re.compile(rf"\b{_ORDINAL}\s+(?:альбом|релиз|сингл|микстейп)\w*\b")

# Прошлое без previous_releases в данных: модели не с чем сравнивать, а она
# сравнивает — «предыдущий альбом» у kizaru, «очередной сольный сингл»,
# «продолжил традицию», «поддерживая свою репутацию, снова напоминает»
# у Ghost Mountain (11.09.2026). С полем «предыдущий, PYREX, вышел в августе» —
# вывод из данных, и эта проверка молчит. «Очередного парада приглашённых»
# не ловится: слово между «очередной» и релизом одно, не больше.
PAST_WITHOUT_DATA = re.compile(
    r"\b(?:предыдущ\w*|в\s+прошлый\s+раз|прошл\w+\s+(?:альбом|релиз|сингл|микстейп)\w*|"
    r"очередн\w+\s+(?:\w+\s+)?(?:альбом|релиз|сингл)\w*|снова|опять|спустя|репутаци\w*|"
    r"продолжа(?:ет|ют|л|ла|ли)|продолжи(?:л|ла|ли|т))\b"
)


def _without_data(text: str, facts: dict) -> str:
    """Текст без слов, взятых из данных: трек «Бит на миллион», «Долги (Freestyle)»,
    имя и жанр магазина — законная цитата, а не выдуманное звучание. Название
    сверяется и целиком, и без хвоста магазина: «Sorry Mama - Single» в посте
    стоит как «Sorry Mama»."""
    names = [
        facts.get("artist"), facts.get("title"), facts.get("subject"), facts.get("genre_by_store"),
        *(facts.get("features") or []),
        *(t.get("title") for t in facts.get("tracks") or []),
        *(r.get("title") for r in facts.get("previous_releases") or []),
    ]
    for name in (n for n in names if isinstance(n, str) and n.strip()):
        for variant in {name, re.split(r"\s[-(\[]", name)[0]}:
            if variant.strip():
                text = re.sub(rf"(?<!\w){re.escape(variant.strip().lower())}(?!\w)", " ", text)
    return text


def _quoted(matches: list[str]) -> str:
    return ", ".join(f"«{m}»" for m in dict.fromkeys(matches))


def problems(text: str, rubric: str, payload: dict | None = None) -> list[str]:
    """Список причин, по которым пост нельзя публиковать. Пусто — годится.

    payload — данные, по которым писался пост: с ними пересказ описи
    сверяется с фактами релиза. Без них эта проверка молчит.
    """
    issues: list[str] = []
    stripped = text.strip()

    if not stripped:
        return ["пустой текст"]

    # Опрос — это JSON по замыслу, к нему текстовые правила не применяются.
    if rubric == "poll":
        return []

    # Мем держится на картинке: две короткие надписи и строка подписи —
    # законный мем, а не брак, поэтому порог у него ниже.
    if len(stripped) < (20 if rubric == "meme" else MIN_LENGTH):
        issues.append(f"слишком короткий ({len(stripped)} знаков)")
    if len(stripped) > MAX_LENGTH:
        issues.append(f"слишком длинный ({len(stripped)} знаков)")

    # Служебный JSON, просочившийся в текст поста.
    if stripped.startswith("{") or '"skip"' in stripped or '"text":' in stripped:
        issues.append("в текст попал служебный JSON")

    # Литеральные «\n» вместо настоящих переводов строки.
    if "\\n" in stripped:
        issues.append("экранированные переводы строк вместо настоящих")

    lowered = stripped.lower()
    for phrase in BANNED_PHRASES:
        if phrase in lowered:
            issues.append(f"запрещённый оборот: «{phrase}»")

    filler_hits = [w for w in FILLER_WORDS if w in lowered]
    if len(filler_hits) >= 2:
        issues.append(f"слова-пустышки: {', '.join(filler_hits)}")

    # Название рубрики, вынесенное в шапку поста. Рубрика и так стоит
    # на карточке, а модель регулярно выносит её отдельной строкой —
    # иногда транслитом («Rubrik Otkuda Nogi»), что читается как брак.
    if re.match(r"\s*(<b>)?\s*(рубрика|rubrik[ao])\b", stripped, re.IGNORECASE):
        issues.append("название рубрики вынесено в шапку поста")

    # Первую строку показывают уведомление и список чатов, поэтому пост
    # начинается с заголовка. Пометку над ним («new tape / 10 треков») убрали:
    # она съедала крючок, а модель по старой памяти ещё может её поставить.
    if rubric in HEADLINED and not stripped.startswith("<b>"):
        issues.append("пост открывается не заголовком <b>")

    # Описи в РЕЛИЗЕ и ВЕРДИКТЕ нет (решение владельца от 11.09.2026): длину
    # показывает плеер под постом, а лишняя строка растягивает пост. GigaChat
    # по старой памяти ставит её обратно или пересказывает прозой: «Один трек,
    # 2:19, никого не позвал». С данными релиза сверяются точное число треков
    # и гостей и хронометраж до секунды. «Ни один трек не дотянул до 3 минут»,
    # «7-минутная вещь», округлённые «2 минуты» и повтор словами проходят:
    # ложная тревога стоит поста — после трёх попыток
    # compose.generate_checked его не выпускает.
    if rubric in ("release", "verdict"):
        if "<code>" in lowered:
            issues.append("опись <code> в посте — её убрали")
        facts = payload or {}
        count = len(facts.get("tracks") or []) or facts.get("track_count")
        guests = len(facts.get("features") or [])
        total = facts.get("total_length")
        checks = [
            (f"{count} трек", rf"(?<!\d){count}[\s-]*трек") if count else None,
            (f"{guests} гост", rf"(?<!\d){guests}[\s-]*гост") if guests else None,
            (total, rf"(?<![\d:]){re.escape(total)}(?![\d:])") if total else None,
        ]
        text_only = _LINK.sub(" ", lowered)
        retold = [name for name, pattern in filter(None, checks) if re.search(pattern, text_only)]
        if retold:
            issues.append("пересказ описи: " + ", ".join(retold))

        # Выдумки — звучание, прошлое и устройство, которых нет в данных.
        # Слова из самих данных (названия, имена, жанр) вырезаются до сверки.
        own = _without_data(text_only, facts)
        sound = INVENTED_SOUND.findall(own)
        if sound:
            issues.append(f"выдуманное звучание: {_quoted(sound)} — релиз никто не слушал, ни звука, ни текстов в данных нет")
        numbers = RELEASE_NUMBER.findall(own)
        if numbers:
            issues.append(f"выдуманный счёт релизов: {_quoted(numbers)} — канал находит не все релизы, "
                          "прошлые называй по именам и не считай")
        # Причина говорит модели, что делать: с полем — держаться его, без поля — молчать.
        past = INVENTED_PAST.findall(own)
        if facts.get("previous_releases"):
            hint = "такого нет ни в каких данных, о прошлом — только названия и даты из previous_releases"
        else:
            past += PAST_WITHOUT_DATA.findall(own)
            hint = "прошлых релизов в данных нет, о прошлом артиста не пиши вовсе"
        if past:
            issues.append(f"выдуманное прошлое: {_quoted(past)} — {hint}")
        # «Все треки короткие, кроме последнего» — у kizaru CA$HEY самый длинный 13-й из 16.
        tracks = facts.get("tracks") or []
        for word, index in (("первого", 0), ("последнего", -1)):
            if tracks and re.search(rf"\bкроме\s+{word}\b", own) and tracks[index].get("length") not in (
                facts.get("longest_track"), facts.get("shortest_track")
            ):
                issues.append(f"выдуманное устройство: «кроме {word}» — этот трек не самый длинный и не самый короткий")

    # Неподдерживаемая разметка, которую Telegram не разберёт.
    if re.search(r"<\s*(br|p|ul|ol|li|h[1-6])\b", stripped, re.IGNORECASE):
        issues.append("неподдерживаемые HTML-теги")

    # Дежурные отмашки вместо отношения. Ловим с любыми пробелами внутри:
    # «ну ок» модель любит ставить в разрядку, и тогда простое вхождение
    # строки его не находит.
    if re.search(r"(?i)\bн\s*у\s+о\s*к\b|\bну\s+такое\b|\bвот\s+такие\s+дела\b", stripped):
        issues.append("дежурная отмашка вместо отношения («ну ок» и подобное)")

    if rubric in RUBRICS_WITHOUT_LINK and re.search(r"<a\s+href", stripped, re.IGNORECASE):
        issues.append("ссылка в рубрике, у которой нет источника — она выдумана")

    # Мем длиной в абзац — это уже не мем.
    if rubric == "meme" and len(stripped) > 400:
        issues.append("мем слишком длинный")

    return issues


def is_ok(text: str, rubric: str) -> bool:
    return not problems(text, rubric)


def _selftest() -> None:
    """Опись, её пересказ и выдумки в РЕЛИЗЕ и ВЕРДИКТЕ ловятся, законное проходит."""
    link = '\n\n▸ <a href="https://music.apple.com/us/album/sorry-mama-single/6807382817">Слушать в Apple Music</a>'
    single = {"tracks": [{"title": "Sorry Mama", "length": "2:19"}], "total_length": "2:19", "features": []}
    album = {"tracks": [{"title": "", "length": "3:11"}] * 22, "total_length": "70:12", "features": list("ABCDEFGHI")}

    def retold(text: str, facts: dict, rubric: str = "release") -> bool:
        return any(p.startswith("пересказ описи") for p in problems(text, rubric, facts))

    # Живой пример от 11.09.2026: опись пересказана в первом абзаце.
    smoky = (
        "<b>SMOKY MO ИЗВИНИЛСЯ ПЕРЕД МАМОЙ</b>\n\n"
        "У Смоки Мо вышел сингл <i>Sorry Mama</i>. Один трек, 2:19, никого не позвал." + link
    )
    assert retold(smoky, single), problems(smoky, "release", single)
    assert retold(smoky, single, "verdict")
    # Разборы бота (service.py) данных не передают — сверять не с чем.
    assert not retold(smoky, {})

    # Заголовок капсом — тоже текст.
    kizaru = "<b>KIZARU ВЫПУСТИЛ 22 ТРЕКА</b>\n\nУ kizaru вышел альбом <i>CA$HEY</i>, короткий и ровный." + link
    assert retold(kizaru, album), problems(kizaru, "release", album)

    # Опись, поставленная по старой памяти.
    assert "опись <code> в посте — её убрали" in problems(smoky + "\n\n<code>1 трек · 2:19</code>", "release")

    # Пост, который владелец сократил в канале сам (Ghost Mountain, 11.09.2026):
    # округлённые «2 минуты» в цитате — довод, а не опись.
    ghost = (
        "<b>GHOST MOUNTAIN ВЫПУСТИЛ СИНГЛ OUTLAST</b>\n\n"
        "Парень из орбиты Haunted Mound, где сингл давно главная форма высказывания: "
        "альбом надо собирать, а трек можно выкинуть в пятницу и уйти.\n\n"
        "<blockquote>2 минуты — это уже не сингл, это проверка, вспомнят ли тебя через неделю.</blockquote>"
    )
    ghost_facts = {**single, "total_length": "2:14"}
    assert problems(ghost, "release", ghost_facts) == [], problems(ghost, "release", ghost_facts)

    # Цифры, которых нет в данных, — вывод из треклиста.
    example = (
        "<b>ARTIST ВЫПУСТИЛ ALBUM И СПРЯТАЛ ГЛАВНОЕ В СЕРЕДИНУ</b>\n\n"
        "Первые 8 треков идут по 1,5 минуты, потом внезапно 7-минутная вещь. "
        "После неё ни один трек не дотянул до 3 минут.\n\n"
        "<blockquote>Столько треков — не щедрость, а отсутствие редактора.</blockquote>" + link
    )
    assert not retold(example, album), problems(example, "release", album)

    # Выдумки от 11.09.2026: звучание и устройство — GigaChat, прошлое — он же,
    # Sonnet («начинал ещё с…») и Haiku («молчал полгода»).
    summer = {**single, "previous_releases": [
        {"title": "От Отца - Single", "released_at": "2026-07-24", "track_count": 1},
        {"title": "Долги (Freestyle) - Single", "released_at": "2026-08-14", "track_count": 1},
        {"title": "PYREX - Single", "released_at": "2026-08-21", "track_count": 1},
    ]}
    cashey = {
        "tracks": [{"title": "BABY DRAC", "length": "2:25"}, {"title": "IN MY DRAWS", "length": "1:38"},
                   {"title": "HOTEL BABY", "length": "3:07"}, {"title": "ALZHEIMER", "length": "3:06"}],
        "shortest_track": "1:38", "longest_track": "3:07", "total_length": "38:16", "features": [],
    }
    invented = [
        ("жёсткий ритм, отрывистый вокал", single), ("звук плотный, рваный", single),
        ("тот самый грув", single), ("русский рэп, построенный на советском звучании", single),
        ("грязный, шипящий, без лишнего блеска", single), ("чистый раппинг поверх сырого бита", single),
        ("СОБРАЛ ВСЮ ЭНЕРГИЮ В ОДНУ ТРЕШОВУЮ МИНУТУ", single),
        # Паузу и возвращение не подтверждает и previous_releases: оно помнит одно лето.
        ("после долгого молчания", summer), ("после серии больших альбомов", summer),
        ("уже третий сингл подряд", summer), ("после серии успешных альбомов", single),
        ("вернулся к корням", summer), ("начинал ещё с «Ю.Г.» в 90-х", summer),
        ("молчал полгода", single),
        ("первый трек начинается ровно с той фразы, которой закончился предыдущий альбом", cashey),
        ("все треки короткие, кроме последнего", cashey),
        # Живой прогон с первым списком прошёл сквозь него — эти обороты добавлены по нему.
        ("третий сингл за лето: после «От Отца» и «PYREX»", summer),
        ("третья часть одноимённой трилогии вышла спустя полгода после первой", cashey),
        ("очередной сольный сингл", single), ("продолжил традицию единичных треков", single),
        ("поддерживая свою репутацию, снова напоминает о себе", single),
        ("первый трек за полгода", single), ("первый трек за осень", single),
        ("ритмы бодрые, голос мощный", cashey), ("тексты про бандитизм и богатство", cashey),
        ("сборник лучших минусовок", cashey),
    ]
    for fragment, facts in invented:
        post = f"<b>SMOKY MO ВЫПУСТИЛ SORRY MAMA</b>\n\nУ него {fragment}.\n\n<blockquote>Вот и всё.</blockquote>"
        for rubric in ("release", "verdict"):
            assert any(p.startswith("выдуманн") for p in problems(post, rubric, facts)), (fragment, rubric)

    # Законное проходит: посты очереди, вычищенные вручную 11.09.2026.
    queue = [
        ("release", summer, "<b>SMOKY MO ВЫПУСТИЛ СИНГЛ SORRY MAMA</b>\n\n"
         "До него за лето вышли <i>От Отца</i>, <i>Долги</i> и <i>PYREX</i>.\n\n"
         "<blockquote>Sorry Mama — это уже не извинение, а рубрика.</blockquote>"),
        ("release", cashey, "<b>KIZARU ВЫПУСТИЛ АЛЬБОМ CA$HEY</b>\n\n"
         "Самый долгий трек длится 3:07, самый короткий — 1:38.\n\n"
         "Так выглядят короткие истории, которые легко повторить дважды.\n\n"
         "<blockquote>Чистый kizaru на весь альбом — редкость в наши дни.</blockquote>"),
        ("release", single, "<b>YННВ ВЫПУСТИЛ ТРЕК СО ЗАБЫТЫМ НАИМЕНОВАНИЕМ</b>\n\n"
         "У YННВ вышел сингл \"Сам придумай этому название\". Видимо, настолько сложно придумать "
         "нормальное название, что проще дать задание фанатам.\n\n"
         "Так выглядит релиз, когда креатив закончился еще вчера.\n\n"
         "<blockquote>Название трека — тест на внимательность.</blockquote>"),
        ("release", single, "<b>OG BUDA & JEKIE DUGN ЗАПИСАЛИ \"ДЖЕКИ БУДЕК\"</b>\n\n"
         "Одному нужна была песня с названием покруче,\nдругому — повод напомнить о себе.\n"
         "Получилась коллаборация длиной в одну песню.\n\n<blockquote>Лучше бы сделали сериал.</blockquote>"),
        ("verdict", {}, "<b>SODA LUV ВЫПУСТИЛ АЛЬБОМ БЕЗ ЕДИНОГО ГОСТЯ</b>\n\n"
         "<i>Прими как есть</i>. Человек, который последние годы появлялся в каждом втором чужом релизе, "
         "здесь не позвал вообще никого.\n\n"
         "Первые 6 вещей уложены между 2:04 и 2:20, будто по линейке. Дальше «Пионы», «Проём» и «Как дела?» — "
         "все за 3 минуты. Хвост длиннее начала: единственное место, где он дал себе растянуться, стоит "
         "в самом конце, а не на витрине.\n\n"
         "<blockquote>Ни одной чужой фамилии — при его-то списке контактов.</blockquote>\n\n"
         "Кто ждал очередного парада приглашённых и панчей на репост — тут этого нет, и кому-то 23 минуты "
         "покажутся наброском, а не альбомом. Я поставил фоном, а на 3-м круге заметил, что ни разу "
         "не перемотал.\n\nа ты когда последний раз дослушивал русский альбом до конца?"),
        ("verdict", single, "<b>NKEEEI & YANIX СДЕЛАЛИ ТРЕК-ПРИЗНАНИЕ БЕЗ ПРОБЛЕМ</b>\n\n"
         "У Nkeeei и Yanix вышел сингл <i>Поцелуи</i>: короткий, простой и совершенно лишённый драмы.\n\n"
         "Эти двое могли бы устроить любовный треугольник века, но обошлись простым признанием:\n\n"
         "<blockquote>Получилось тепло и искренне. Без пафоса и драм.</blockquote>\n\n"
         "Да, такой формат сегодня редкость. Да, многие ждут конфликта и интриги. "
         "Но ведь признание — это тоже история, правда?"),
    ]
    for rubric, facts, post in queue:
        assert problems(post + link, rubric, facts) == [], (post[:40], problems(post + link, rubric, facts))

    # Прошлое из данных проходит, без данных — нет; «бит» в названии трека — цитата, а не звук.
    counted = ("<b>SMOKY MO ВЫПУСТИЛ СИНГЛ SORRY MAMA</b>\n\n"
               "Предыдущий, <i>PYREX</i>, вышел в августе, а до него — <i>Долги (Freestyle)</i>.\n\n"
               "<blockquote>Извинение по расписанию.</blockquote>")
    assert problems(counted, "release", summer) == [], problems(counted, "release", summer)
    assert any(p.startswith("выдуманное прошлое") for p in problems(counted, "release", single))
    beat = {**single, "tracks": [{"title": "Бит на миллион", "length": "2:19"}]}
    titled = ("<b>SMOKY MO ВЫПУСТИЛ БИТ НА МИЛЛИОН</b>\n\n"
              "<i>Бит на миллион</i> вышел в пятницу, и название обещает больше, чем 2 минуты.\n\n"
              "<blockquote>Миллион пока только в названии.</blockquote>")
    assert problems(titled, "release", beat) == [], problems(titled, "release", beat)
    print("quality: самопроверка пройдена")


if __name__ == "__main__":
    import argparse

    parser = argparse.ArgumentParser(description="Отбраковка постов перед очередью.")
    parser.add_argument("--selftest", action="store_true", help="опись, выдуманные звук и прошлое ловятся, законное проходит")
    if parser.parse_args().selftest:
        _selftest()
